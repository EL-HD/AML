# Versiones fijadas (S-12). Probadas con Python 3.11 (Dockerfile) y 3.13 (entorno local).
# Actualice con: pip install -r requirements.txt && pip-audit -r requirements.txt
#
# 2026-09-22: cryptography, PyJWT y python-multipart elevados al minimo que
# cierra los 21 avisos reportados por pip-audit en el CI (OWASP A06, y A02 por
# el componente criptografico). Versiones de correccion tomadas de la columna
# "Fix Versions" de pip-audit; el salto de cryptography abarca varias versiones
# mayores, por lo que la validacion recae en las 245 pruebas del CI.

# Frontend y análisis
streamlit==1.55.0
pandas==2.3.3
numpy==2.4.3
plotly==6.6.0
matplotlib==3.10.8
openpyxl==3.1.5
networkx==3.6.1
requests==2.33.0
python-docx==1.2.0
reportlab==4.4.10

# Backend y API
fastapi==0.136.1
uvicorn==0.46.0
sqlalchemy==2.0.49
psycopg2-binary==2.9.12
pydantic==2.13.4
python-multipart==0.0.31
email-validator==2.3.0
PyJWT==2.13.0
bcrypt==5.0.0
cryptography==50.0.0
# Opcional (T6 MFA): código QR de enrolamiento; sin esta librería la UI muestra la clave manual.
qrcode==8.2
