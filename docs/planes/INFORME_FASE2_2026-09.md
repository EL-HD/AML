# Informe de Ejecución: Fase 2 Cumplimiento, Seguridad y Analítica · Sovereign AML

**Plan de referencia:** `docs/planes/PLAN_FASE2_2026-09.md` · **Rama:** `mejora/fase2-cumplimiento-2026-09` (sin push, sin merge).
Cada tarea añade su sección al terminar (estado, commit, evidencia, variables nuevas y pasos de despliegue).

## Resumen ejecutivo (cierre de fase, 17/09/2026)

Rama `mejora/fase2-cumplimiento-2026-09`, 19 commits sobre `main` (código + informe por tarea), **sin push, sin merge y sin cambios en Railway**. Todas las tareas del plan quedaron HECHAS; los puntos que solo pueden validarse con red, Docker o navegador real se listan en "Riesgos residuales".

### Estado por tarea

| Tarea | Estado | Commit de código | Entregable principal |
|-------|--------|------------------|----------------------|
| T1 Persistencia de casos (Art. 29, 30, 34) | HECHO | `fbab9e9` | `CasosAlerta` + historial inmutable, flujo cuatro ojos, migración 004 |
| T2 CI en GitHub Actions | HECHO (primera corrida real pendiente del push) | `d611307` | `.github/workflows/ci.yml`, `bandit.yaml`, Dependabot, guía "Wait for CI" |
| T3 Screening de sanciones (GAFI R.6/R.7) | HECHO | `dc66328` | `backend/screening.py`, migración 005, vista Listas de Sanciones |
| T4 Bitácora a prueba de alteraciones | HECHO | `a959f08` | Cadena SHA-256/HMAC por licencia, migración 006, vista Integridad |
| T5 Cabeceras de seguridad | HECHO (build de imagen y navegador real por observar) | `15baaba` | Caddy con CSP/HSTS delante de Streamlit, `verificar_cabeceras.py` |
| T6 MFA TOTP (RFC 6238) | HECHO | `890b8b2` | `backend/totp.py`, `backend/mfa.py`, migración 007, vista Seguridad de la Cuenta |
| T7 Respaldos | HECHO (cron documentado, S3 sin probar) | `3e9be0f` | `respaldo_bd.py`/`restaurar_bd.py` cifrados, manifiesto firmado, runbook |
| T8 Detección de anomalías | HECHO | `30e2cab` | `backend/anomalias.py` (Isolation Forest numpy + MAD), pestaña y PDF |
| T9 Pendientes UI | HECHO | `1aedba4` | Moneda configurable en PDF/ejes, `style=` inline 156 -> 2 |

**Pruebas:** 70 al inicio de la fase -> **245 en verde** (`python -m unittest discover -s tests`), más prueba de humo `AppTest` en las 17 vistas, LOGIN y SIN_DATOS. **Duplicación:** 1.54 % (umbral 10 %). Migraciones validadas con `psql` en PostgreSQL 16, dos veces cada una (idempotencia).

### Variables de entorno nuevas en la fase

| Variable | Tarea | Obligatoria | Valor por defecto | Uso |
|----------|-------|-------------|-------------------|-----|
| `SCREENING_AUTO_DOWNLOAD` | T3 | Opcional | `false` | Habilita la descarga desde las URLs oficiales fijas de OFAC y ONU (la carga manual funciona siempre). |
| `AUDIT_HMAC_KEY` | T4 | Opcional, **recomendada** en producción | vacía (se usa SHA-256) | Clave HMAC-SHA256 de los eslabones de la bitácora. Definirla **antes** del primer despliegue y no cambiarla ni retirarla después. |
| `CSP_SCRIPT_EXTRA` | T5 | Opcional | vacía | Fuentes extra para `script-src` de la CSP (válvula de escape sin reconstruir la imagen). |
| `PORT` | T5 | La fija Railway | `8080` en la imagen (antes 8501) | Puerto en el que escucha Caddy; Streamlit queda en `127.0.0.1:8501`. |
| `MFA_ENCRYPTION_KEY` | T6 | Opcional; **obligatoria para habilitar MFA** | vacía (no se puede enrolar) | Clave Fernet para cifrar los secretos TOTP. Generar con `Fernet.generate_key()`; no cambiar con usuarios enrolados. |
| `MFA_ENFORCE` | T6 | Opcional | `false` | `true` exige MFA a `admin` y `oficial`. |
| `BACKUP_ENCRYPTION_KEY` | T7 | **Obligatoria para respaldar/restaurar** (solo en el servicio cron o donde corran los scripts) | ninguna | Clave maestra (formato Fernet) del cifrado AES-256-GCM y de la firma del manifiesto. Generar con `python scripts/respaldo_bd.py --generar-clave`; guardar copia en el gestor de secretos. |
| `BACKUP_DIR` | T7 | Opcional | `backups/` | Destino local (en Railway, un volumen, por ejemplo `/data/respaldos`). |
| `BACKUP_S3_BUCKET`, `BACKUP_S3_ENDPOINT`, `BACKUP_S3_REGION` | T7 | Opcionales (alternativa a `BACKUP_DIR`) | vacías | Destino S3 compatible (Railway Bucket); requieren `boto3`. |
| `BACKUP_S3_PREFIX` | T7 | Opcional | `sovereign-aml` | Prefijo de los objetos en el bucket. |
| `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` | T7 | Obligatorias solo con S3 | ninguna | Credenciales del bucket. |
| `BACKUP_RETENCION_DIARIOS` / `_SEMANALES` / `_MENSUALES` | T7 | Opcionales | `7` / `4` / `12` | Política abuelo-padre-hijo. |
| `BACKUP_TMPDIR` | T7 | Opcional | temporal del sistema | Directorio privado donde vive el volcado en claro durante segundos. |
| `BACKUP_HOSTS_PRODUCCION` | T7 | Opcional | vacía | Hosts adicionales considerados producción al restaurar. |
| `BACKUP_PERMITIR_RESTAURAR_PRODUCCION` | T7 | Solo durante una restauración de producción autorizada | no definida | Debe valer `si` junto con `--confirmar-produccion`; retirarla al terminar. |

T1, T2, T8 y T9 no añaden variables. T8 añade claves de configuración AML (`anomalia_*`) con valores por defecto.

### Orden de despliegue seguro a producción

1. **Antes de desplegar, crear en Railway** (servicio de la aplicación): `AUDIT_HMAC_KEY` (32+ caracteres aleatorios) y `MFA_ENCRYPTION_KEY` (Fernet). Guardar ambas en el gestor de secretos de la organización. Dejar `MFA_ENFORCE` sin definir (o `false`) y `CSP_SCRIPT_EXTRA` vacía. No definir `SCREENING_AUTO_DOWNLOAD` todavía.
2. **Activar "Wait for CI"** en Railway (Settings > Source > Wait for CI) y hacer el push de la rama / PR a `main`: la primera corrida real de `ci.yml` (pruebas, bandit, pip-audit, duplicación, migraciones en PostgreSQL 16) puede revelar hallazgos de bandit/pip-audit que no pudieron ejecutarse localmente (PyPI bloqueado); corregirlos antes del merge. Con "Wait for CI" activo, `main` solo despliega en verde.
3. **Despliegue del código**: al arrancar, `scripts/db_migrate.py` aplica en orden **004 (casos), 005 (screening), 006 (bitácora con hash), 007 (MFA)**; son idempotentes y coexisten con `create_all`. Importante: código y migración 006 deben ir juntos (ya ocurre con el runner): tras 006 la base rechaza inserciones en la bitácora sin cadena. Vigilar el build: el stage `builder` descarga Caddy desde GitHub y verifica SHA-512; si la red de build lo impide, el build falla de forma explícita (ver reversión de T5).
4. **Verificación inmediata tras el despliegue**: (a) `python scripts/verificar_cabeceras.py https://<dominio> --websocket` debe terminar con "Todas las comprobaciones se cumplen"; (b) abrir la aplicación con la **consola del navegador** visible y recorrer login, carga de Excel, gráficos Plotly, exportaciones PDF/CSV y manual: no debe aparecer ningún "Refused to ... Content Security Policy"; si aparece, añadir la fuente (o `'unsafe-eval'`) en `CSP_SCRIPT_EXTRA` y redesplegar sin tocar código; (c) Administración > Integridad de Bitácora > "Verificar cadena ahora": cadena íntegra con HMAC activo; conservar el CSV como línea base; (d) generar un RTE y comprobar la moneda de trabajo y el umbral "USD 10,000".
5. **Listas de sanciones**: un Administrador descarga `sdn.csv`, `alt.csv` (OFAC) y `consolidated.xml` (ONU) de los sitios oficiales y los carga en Investigación > Listas de Sanciones; validar que los parsers aceptan los archivos reales (no pudieron descargarse en desarrollo). Solo entonces, y si la red de Railway alcanza `www.treasury.gov` y `scsanctions.un.org`, considerar `SCREENING_AUTO_DOWNLOAD=true`.
6. **Activación de MFA_ENFORCE** (en dos pasos): con `MFA_ENCRYPTION_KEY` ya definida, cada Administrador y Oficial enrola su autenticador en Administración > Seguridad de la Cuenta y guarda sus códigos de recuperación; comprobar que al menos **dos administradores** están enrolados y que un cierre/reinicio de sesión y una recarga del navegador funcionan con código. Solo entonces poner `MFA_ENFORCE=true` y redesplegar: los admin/oficial no enrolados verán únicamente la pantalla de enrolamiento en su próximo login; analistas y auditores no cambian.
7. **Servicio cron de respaldos**: (a) añadir al `Dockerfile` (stage `runtime`) `postgresql-client-18` con el snippet de la sección T7 y, si se usa bucket, `boto3` en `requirements.txt`; verificar el build; (b) crear en el mismo proyecto un servicio "New > GitHub Repo" con la misma rama y "Config file path" = `railway.respaldo.toml` (`cronSchedule = "0 3 * * *"`, sin dominio público); (c) variables del cron: `DATABASE_URL=${{Postgres.DATABASE_URL}}`, `BACKUP_ENCRYPTION_KEY` (guardada en el gestor de secretos) y un destino (`BACKUP_DIR` con volumen en `/data`, o `BACKUP_S3_*` + credenciales del Bucket); (d) primera ejecución manual con `--sin-retencion`, luego `--listar` y `python scripts/restaurar_bd.py --respaldo <último> --verificar-solo`; (e) programar la verificación mensual y el ensayo trimestral del runbook, y comparar las anclas de auditoría del manifiesto con "Integridad de Bitácora".
8. **Gobierno del modelo de anomalías** (T8): registrar la precisión observada contra casos examinados antes de usar la señal para priorizar y fijar la recalibración trimestral.

### Riesgos residuales

- **CSP en navegador real** (T5): validada por análisis de bundles, no con navegador contra el proxy; mitigación inmediata `CSP_SCRIPT_EXTRA`; riesgo de bloqueo de algún componente hasta la verificación del paso 4.
- **Build de la imagen** (T5, T7): no hay Docker en los entornos de trabajo; la descarga verificada de Caddy y la instalación de `postgresql-client-18`/`qrcode`/`boto3` se validan en el primer build de Railway. Si `qrcode` rompiera el build, retirarlo de `requirements.txt` (MFA funciona con clave manual).
- **bandit / pip-audit** (T2): no ejecutados localmente; posibles hallazgos en la primera corrida de CI.
- **Archivos reales OFAC/ONU** (T3): parsers probados con fixtures conforme al formato documentado; un cambio de etiquetas en la ONU exigiría ajustar `parsear_onu_consolidada`.
- **Estado en memoria de un solo worker** (T6, límite de intentos y `jti` canjeados; heredado del limitador previo): con varios workers migrar a Redis.
- **Claves irrecuperables**: perder `MFA_ENCRYPTION_KEY` obliga a restablecer el MFA de todos (entran con códigos de recuperación); perder `BACKUP_ENCRYPTION_KEY` deja los respaldos indescifrables; retirar `AUDIT_HMAC_KEY` vuelve no verificables los eslabones HMAC. Las tres deben estar en el gestor de secretos con doble custodia.
- **Borrado de la cola de la cadena de bitácora** (T4): solo se detecta comparando el último hash entre verificaciones o con las anclas del manifiesto de respaldo (T7); los triggers lo impiden a nivel de base.
- **S3 y cron no ejercitados** (T7): código conforme a la API de boto3 y a la configuración documentada de Railway; primera corrida con `--sin-retencion`.
- **Moneda** (T9): sin tipo de cambio; la moneda de trabajo etiqueta los montos tal como llegan; el umbral RTE se compara numéricamente con el monto del Excel.

### Pasos de reversión

- **General**: Railway conserva el despliegue anterior si el nuevo no supera el healthcheck; para volver atrás, redesplegar el commit previo de `main` (o `git revert` de los commits de la fase, en orden inverso). Las migraciones 004-007 son aditivas (tablas y columnas nuevas, triggers) y no rompen el código anterior salvo lo indicado abajo.
- **T5 (proxy)**: `git revert 15baaba`, o restaurar en `supervisord.conf` `--server.port %(ENV_PORT)s --server.address 0.0.0.0` y quitar `[program:caddy]`, y en `Dockerfile` `PORT=8501`, `EXPOSE 8501` y el healthcheck a `localhost:8501`. Sin efecto en datos.
- **T6 (MFA)**: poner `MFA_ENFORCE=false` desactiva la obligatoriedad de inmediato; los usuarios ya enrolados siguen con MFA. Para desactivar MFA de un usuario, un administrador lo restablece desde la vista; retirar `MFA_ENCRYPTION_KEY` solo impide nuevos enrolamientos (los enrolados entran con códigos de recuperación). La tabla `MfaUsuarios` puede quedar sin uso.
- **T4 (bitácora)**: si se revierte el código a una versión sin cadena, **debe revertirse también la migración 006** (eliminar `trg_bitacora_validar_eslabon` y `trg_bitacora_inmutable`) o la aplicación anterior no podrá insertar en la bitácora; las columnas `seq/hash_prev/hash/hash_alg` pueden quedar. Hacerlo solo por procedimiento del DBA y documentarlo, porque desactiva la protección append-only.
- **T3 / T1 (screening, casos)**: revertir el código deja las tablas `ListasSancion*`, `Screening*`, `CasosAlerta*` sin uso; no hay que borrarlas (retención Art. 34).
- **T7 (respaldos)**: eliminar el servicio cron en Railway; los respaldos existentes siguen siendo restaurables con la clave de su fecha.
- **T9 (UI)**: `git revert 1aedba4`; sin datos ni variables afectadas.
- **T2 (CI)**: desactivar "Wait for CI" en Railway si el pipeline bloqueara un despliegue urgente; el workflow puede deshabilitarse desde GitHub sin tocar el código.

---

---

## T1 Persistencia de casos (Art. 29, 30 y 34 Ley 6593)

**Estado:** HECHO · **Fecha:** 17/09/2026 · **Commit:** `fbab9e9` (código) y el commit de este informe.

### Qué se hizo
- **Modelo de datos** (`backend/models.py`, `migrations/004_casos_alerta.sql`):
  - `public."CasosAlerta"`: estado vigente por caso con `licenciaid`, `clave_caso`, `hash_lote`, `cliente`, `nombre_archivo`, `estado` (CHECK con los cinco estados), `fundamento`, `score_max`, `nivel_riesgo`, `propuesto_por/en`, `aprobado_por/en`, `fecha_clasificacion_sospechosa`, trazas de creación y actualización. `UNIQUE (licenciaid, clave_caso)` e índice `(licenciaid, hash_lote)`.
  - `public."CasosAlertaHistorial"`: historial append-only de transiciones (`estado_anterior`, `estado_nuevo`, `accion`, `fundamento`, `usuario`, `rol`, `registrado_en`), con FK a `CasosAlerta`.
  - Retención (Art. 34): triggers PostgreSQL que rechazan `DELETE` en ambas tablas y `UPDATE` en el historial; en el ORM, listeners `before_delete`/`before_update` lanzan `HistorialInmutableError` (también efectivos en SQLite). No existe ninguna función ni control de UI que borre casos.
  - La migración es idempotente (`IF NOT EXISTS`, `CREATE OR REPLACE FUNCTION`, `DROP TRIGGER IF EXISTS`) y los nombres de índice coinciden con los que genera `create_all`, por lo que la ruta real de `scripts/db_migrate.py` (ORM primero, SQL después) no crea índices duplicados.
- **Clave estable del caso** (documentada en `backend/casos_alerta.py` y en la cabecera de la migración):
  - `hash_lote` = SHA-256 del lote canónico de transacciones (columnas ordenadas, filas ordenadas, todo como texto, CSV UTF-8). No depende del nombre del archivo ni del orden de filas.
  - `clave_caso` = SHA-256 de `licenciaid|hash_lote|cliente_normalizado` (cliente recortado, sin espacios repetidos, en mayúsculas).
  - El hash se calcula una vez por lote cargado y se memoriza en la sesión (`frontend/casos_persistencia.py`); se olvida con "Nuevo análisis".
- **Flujo de cuatro ojos** (`backend/casos_alerta.py`, tabla `TRANSICIONES`):
  - Roles operativos (admin, oficial, analista) examinan, descartan o proponen `Sospechosa_Propuesta` (nuevo estado intermedio) con fundamento obligatorio (mínimo 10 caracteres, Art. 29).
  - Solo admin u oficial aprueban `Sospechosa_Confirmada`, y nunca quien propuso (comparación de usuario normalizado). `Sospechosa_Confirmada` es terminal; el RTS se emite en Reportes.
  - Una propuesta puede retirarla el proponente o rechazarla un oficial/admin. El auditor solo observa.
  - `permisos.py`: acciones nuevas `proponer_caso_sospechoso` (admin, oficial, analista) y `aprobar_caso_sospechoso` (admin, oficial). La autoridad real está en el backend (`_validar_transicion`), que recibe rol y usuario desde la sesión autenticada.
- **Auditoría:** cada transición registra `CAMBIO_ESTADO_CASO:<anterior>-><nuevo>` en `BitacoraAuditoria` (módulo "Casos de Alerta") además del historial propio del caso.
- **UI** (`frontend/mod_alertas.py`, `app.py`):
  - Al haber un análisis cargado, `app.py` rehidrata in situ el DataFrame `casos` con los estados guardados para (licencia, lote); así Casos de Alerta, Resumen Ejecutivo (KPIs) y Reportes (RTS) ven el mismo estado sin pasar por la vista de alertas.
  - El panel de gestión crea el caso persistido al seleccionarlo, muestra clave, estado, proponente y aprobador, ofrece únicamente las transiciones autorizadas para el rol y usuario actuales, y lista el historial inmutable.
  - Manual de usuario actualizado con el estado `Sospechosa_Propuesta`, la regla de cuatro ojos y la retención.

### Evidencia de pruebas (contenedor cloud, Python 3.11)
- `python3 -m unittest discover -s tests`: **89 pruebas en verde** (70 previas + 19 nuevas en `tests/test_casos_alerta.py`, más una en `test_permisos.py`). Cobren: hash de lote estable ante reorden de filas y columnas y sensible a cambios de datos; idempotencia de `obtener_o_crear_caso`; aislamiento por tenant (otra licencia no lee, no muta ni ve historial); analista propone pero no confirma; el proponente no aprueba aunque sea oficial; oficial distinto aprueba y queda trazado; no se confirma sin propuesta; auditor sin transiciones; fundamento obligatorio; historial append-only (UPDATE y DELETE rechazados); caso no borrable; auditoría `CAMBIO_ESTADO_CASO`; rehidratación sobre el DataFrame sin mezclar tenants.
- Migración validada con `psql` (PostgreSQL 16) en dos rutas: (a) base con 001-002 aplicadas y 004 dos veces seguidas (idempotente, sin error); (b) DDL generado por el ORM y luego 004 (sin índices duplicados: 8 índices esperados). Con datos de prueba, los triggers rechazaron `UPDATE`/`DELETE` del historial y `DELETE` del caso, y el CHECK rechazó un estado inválido. Nota: 003 falla en una base vacía porque `Licencias` la crea el ORM; es comportamiento previo y no afecta a T1.
- Prueba de humo `apptest_smoke.py` en "Casos de Alerta", "Resumen Ejecutivo", "Reportes" y "SIN_DATOS": 0 excepciones, 0 errores, sin XSS. Prueba de humo adicional end-to-end con `AppTest` (sesión analista propone, nueva sesión del mismo analista no ve `Sospechosa_Confirmada`, sesión de oficial distinto la ve y confirma): estado rehidratado en la tabla de la segunda sesión, historial `CREAR -> PROPONER_SOSPECHOSA -> APROBAR_SOSPECHOSA` y dos eventos de auditoría.
- Duplicación de código: 1.97 % (umbral 10 %).

### Variables nuevas
Ninguna.

### Pasos de despliegue
1. `scripts/db_migrate.py` se ejecuta al arrancar y aplica `004_casos_alerta.sql` (requiere `pgcrypto`, ya usado por 001).
2. No se requiere acción manual. Si la base ya tenía las tablas creadas por `create_all` (arranque previo con este código), la migración solo añade triggers y comentarios.

### Pendientes y decisiones
- `Sospechosa_Confirmada` es terminal por diseño: revertir una confirmación exigiría un flujo de anulación con doble aprobación que no está en el alcance de T1. Opción: tarea futura "anulación de RTS" con fundamento y aprobación cruzada.
- Purga por retención (más de 5 años) queda fuera del alcance: la aplicación no borra; una eventual purga debe hacerse por procedimiento del administrador de base de datos deshabilitando temporalmente el trigger.
- La rehidratación filtra por `hash_lote`: si el lote cambia (una fila más), los casos previos no se recuperan por diseño (la evidencia del examen corresponde a un lote concreto). Los casos anteriores siguen consultables en la base de datos.

---

## T2 CI en GitHub Actions

**Estado:** HECHO (validación local completa; la primera ejecución real en GitHub queda pendiente del push) · **Fecha:** 17/09/2026 · **Commit:** ver `git log` (commit `ci(t2): ...`).

### Qué se hizo
- **`.github/workflows/ci.yml`**: disparadores `push` y `pull_request` a `main` y `mejora/**`; `permissions: contents: read`; `concurrency` por rama con `cancel-in-progress`; `timeout-minutes` en cada trabajo; sin secretos (solo las variables ficticias de `tests/conftest.py`: `SECRET_KEY`, `SESSION_SIGN_KEY`, `JWT_ISSUER`, `JWT_AUDIENCE`, `CACHE_ENCRYPTION_SALT`, `DATABASE_URL=sqlite://`).
  - Trabajo **calidad**: `actions/setup-python` con Python 3.11 y caché de pip (`cache-dependency-path: requirements.txt`); `pip install -r requirements.txt bandit pip-audit`; `python -m unittest discover -s tests -v`; `bandit -c bandit.yaml -r backend frontend auth_api.py app.py scripts -ll`; `pip-audit -r requirements.txt --progress-spinner off`; `python scripts/medir_duplicacion.py --umbral 10 app.py auth_api.py backend frontend scripts` (sale con código 1 si supera 10 %).
  - Trabajo **migraciones**: servicio `postgres:16` con `pg_isready` como healthcheck y credenciales efímeras de CI; `scripts/db_migrate.py` dos veces sobre base vacía; la segunda ejecución debe imprimir "Sin migraciones SQL pendientes" (`set -o pipefail` para que un fallo en la tubería no quede oculto); luego `psql` verifica que `schema_migrations` tiene tantas filas como archivos en `migrations/`, que existe `Licencias.rol` y la restricción `licencias_rol_check`.
- **`bandit.yaml`**: excluye `tests/`, entornos locales y respaldos (evita falsos positivos por credenciales ficticias y `assert` en pruebas).
- **`.github/dependabot.yml`**: actualizaciones mensuales de las acciones del workflow.
- **`scripts/medir_duplicacion.py`**: la huella de ventanas pasa de `sha1` a `sha256`. No es un uso criptográfico, pero `bandit -ll` reporta B324 (severidad media) para `sha1`/`md5` y haría fallar el pipeline. Resultado idéntico (1.72 %).
- **README**, sección 7: descripción del pipeline y pasos para activar **Wait for CI** en Railway (Settings > Source > Wait for CI / Check Suites). No se tocó la configuración de Railway.

### Decisiones
- **Fijación de acciones a versión mayor** (`actions/checkout@v4`, `actions/setup-python@v5`) en lugar de SHA de commit: desde el entorno de desarrollo no hay acceso a `api.github.com` ni a `github.com` (proxy 403), por lo que no fue posible verificar los SHA; un SHA erróneo rompería todo el pipeline. La decisión y el procedimiento para migrar a SHA (aceptar el primer PR de Dependabot y usar `uses: owner/accion@<sha> # vX.Y.Z`) quedan documentados en la cabecera del workflow.
- **Orden del runner de migraciones**: se revisó `scripts/db_migrate.py`: `models.Base.metadata.create_all` corre antes que `migrations/*.sql`, de modo que `Licencias` (con `rol`) ya existe cuando se aplica `003_rol_licencias.sql`. El problema conocido ("003 falla en base vacía") solo ocurre al aplicar los `.sql` con `psql` sin pasar por el runner; con `db_migrate.py` no se reproduce, por lo que no hubo que corregir nada en el runner ni en las migraciones 001-003. El trabajo `migraciones` de CI cubre exactamente esa ruta.

### Evidencia (contenedor cloud, Python 3.11, PostgreSQL 16)
- YAML de los tres archivos cargado con `yaml.safe_load` sin errores; estructura verificada (2 trabajos, 7 y 6 pasos, disparadores y permisos correctos).
- `python3 -m unittest discover -s tests`: 89 pruebas en verde.
- `scripts/medir_duplicacion.py --umbral 10 app.py auth_api.py backend frontend scripts`: 9073 líneas, 156 duplicadas, 1.72 % (código de salida 0).
- Emulación del trabajo `migraciones` con `psql` (sin `psycopg2` en el contenedor): DDL del ORM generado con el dialecto PostgreSQL y aplicado a una base vacía, después `001` a `004` dos veces seguidas sin error; comprobaciones finales del paso "Verificar esquema resultante" (4 = 4 migraciones registradas, columna `rol` presente, CHECK presente) satisfechas.
- Revisión estática previa a bandit sobre los mismos patrones que reporta con severidad media (`requests` sin `timeout`, `/tmp` literal, `0.0.0.0`, `yaml.load`, `pickle`, `eval/exec`, `shell=True`, SQL por f-string, `md5/sha1`): único hallazgo el `sha1` de `medir_duplicacion.py`, corregido.

### Variables nuevas
Ninguna en producción. En CI solo valores ficticios definidos en el propio workflow.

### Pasos de despliegue
1. Al hacer push de la rama, GitHub ejecutará el workflow automáticamente (el push queda fuera del alcance de esta fase, según las reglas comunes).
2. Activar **Wait for CI** en Railway (Settings > Source) para que `main` solo despliegue con CI en verde.

### Pendientes y motivo
- `bandit` y `pip-audit` no pudieron ejecutarse localmente (PyPI bloqueado en ambos entornos); la primera corrida en GitHub puede revelar hallazgos. Opciones si ocurre: corregir el código (preferible), o añadir `# nosec Bxxx` con justificación puntual; para `pip-audit`, actualizar la versión afectada en `requirements.txt` o, si no existe parche, `--ignore-vuln <id>` documentado.
- Fijación a SHA de commit: pendiente hasta el primer PR de Dependabot (ver Decisiones).

---

## T3 Screening de listas de sanciones (GAFI R.6 / R.7)

**Estado:** HECHO · **Fecha:** 17/09/2026 · **Commit:** `dc66328` (código) y el commit de este informe.

### Qué se hizo
- **Motor puro** (`backend/screening.py`, sin Streamlit ni base de datos):
  - Normalización: NFKD sin acentos, minúsculas, puntuación a espacio, tokens ordenados, eliminación de formas jurídicas y partículas (S.A., LTD, de, la...) y de siglas de una letra, salvo que no quede ningún token.
  - Jaro-Winkler implementado con biblioteca estándar (p = 0.1, prefijo máximo 4) y verificado contra valores de referencia (MARTHA/MARHTA 0.9611, DWAYNE/DUANE 0.8400, DIXON/DICKSONX 0.8133).
  - Puntaje = máximo entre Jaro-Winkler del nombre completo normalizado y una similitud por tokens (promedio del mejor Jaro-Winkler por token, atenuado por la diferencia de cantidad de tokens: 0.8 + 0.2 × corto/largo). Umbral configurable entre 0.70 y 1.00, por defecto **0.88**. Resultado explicable: fuente, referencia, nombre de la entrada, alias coincidente, puntaje y motivo ("Coincidencia exacta del nombre normalizado", "Jaro-Winkler 0.914 sobre el nombre completo", "Similitud por tokens 0.900 (tokens comunes: juan, perez)").
  - `IndiceScreening`: índice invertido por token y por prefijo de 3 caracteres sobre nombres y alias; cada consulta solo se compara contra los candidatos que comparten token o prefijo.
  - Parsers con validación de filas: OFAC SDN (`sdn.csv` de 12 columnas y `alt.csv` de 5, sin encabezado, `-0-` como nulo, alias `aka/fka/nka`, filas inválidas o duplicadas contadas como rechazadas) y ONU consolidada (XML: `INDIVIDUAL`/`ENTITY`, `DATAID`, nombres `FIRST_NAME..FOURTH_NAME`, `UN_LIST_TYPE`, `NATIONALITY`, alias separados por `;`).
  - Endurecimiento XML: tamaño máximo 50 MB y rechazo de cualquier `<!DOCTYPE` o `<!ENTITY` (XXE y "billion laughs") **antes** de llamar a `xml.etree`. Los `# nosec` puntuales (B405/B314) quedan justificados en el código.
  - Descarga oficial opcional: solo desde las constantes `URL_OFAC_SDN`, `URL_OFAC_ALT` y `URL_ONU_CONSOLIDADA` (hosts `www.treasury.gov` y `scsanctions.un.org`), HTTPS, `allow_redirects=False`, timeout 60 s, lectura por trozos con corte al superar 50 MB. Desactivada por defecto; se habilita con `SCREENING_AUTO_DOWNLOAD=true`. Nunca se acepta una URL del usuario (anti SSRF). El contenedor de pruebas no tiene red: la ruta real no se ejercita; se prueba con un doble de `requests.get`.
- **Persistencia** (`migrations/005_screening.sql`, `backend/models.py`, `backend/screening_repo.py`):
  - `ListasSancion` (global): fuente (CHECK `OFAC_SDN`/`ONU`), versión, archivo, `hash_sha256`, cantidad de entradas, filas rechazadas, origen (`carga_manual`/`descarga_oficial`), activa, quién y cuándo. Solo una versión activa por fuente; una versión con el mismo SHA-256 activo se rechaza.
  - `ListasSancionEntradas` (global): referencia, nombre, nombre normalizado, tipo, programa, nacionalidad y alias en JSON. Inserción por lotes de 2000.
  - `ScreeningCoincidencias` (por `licenciaid`): cliente, origen (`Cliente`/`Cliente_Destino`), `hash_lote`, lista, entrada, fuente, referencia, nombre de lista, alias, puntaje, motivo, estado (CHECK `Pendiente`/`Descartada`/`Confirmada`), fundamento, revisor, fecha, `caso_id`. `UNIQUE (licenciaid, cliente_normalizado, entrada_id)`: repetir el screening no duplica ni pisa decisiones.
  - `ScreeningDecisiones` (por `licenciaid`): historial append-only de cada decisión (trigger PostgreSQL y listeners ORM que rechazan UPDATE/DELETE, misma función `fn_casos_alerta_inmutable` de la migración 004).
  - Decisión (`decidir`): solo admin/oficial; fundamento obligatorio (10 a 4000 caracteres); `Descartada` o `Confirmada`; al confirmar un cliente del lote se obtiene o crea su Caso de Alerta (T1) y se guarda `caso_id`; para contrapartes (`Cliente_Destino`) queda la señal sin caso. Cada decisión registra `DECISION_SCREENING:<anterior>-><nuevo>` en `BitacoraAuditoria`; la carga registra `CARGA_LISTA_SANCION:<fuente>:<n>` y la ejecución `EJECUCION_SCREENING:<nuevas>`.
  - Índice cacheado por proceso mientras no cambie el conjunto de listas activas (se invalida en cada carga).
- **Permisos** (`frontend/permisos.py`): `cargar_listas_sancion` (admin), `gestionar_screening` (admin, oficial), `ver_screening` (todos). La autoridad real está en `screening_repo` (`ROLES_CARGA`, `ROLES_REVISION`).
- **UI** (`frontend/mod_screening.py`, `frontend/navegacion.py`, `app.py`, `frontend/mod_cliente.py`):
  - Vista **Investigación > Listas de Sanciones** (no requiere análisis cargado) con tres pestañas: listas cargadas y carga (extensión, tamaño y contenido validados; panel de descarga oficial visible solo si la variable está activa), ejecución sobre `Cliente` y `Cliente_Destino` del análisis con umbral ajustable, y bandeja con KPIs, tabla, detalle explicable, historial y formulario de decisión. Analista y auditor ven todo en solo lectura (`exigir_o_avisar`).
  - `app.py` añade la columna `Screening_Sanciones` al DataFrame de casos en cada ejecución (`marcar_casos`, señales cacheadas en sesión y refrescadas tras ejecutar o decidir).
  - Ficha del cliente: insignia "Screening de sanciones: Pendiente/Confirmada (n)" o "Sin coincidencias en listas de sanciones" y resumen de coincidencias.
  - Todo valor dinámico pasa por `ui_safe.h`, `html_block` o `render_html_table`; se reutilizan `page_header`, `kpi`, `info_panel`, `status_badge`, `empty_state`.
- Manual de usuario (sección 5.3) y README (variable nueva) actualizados.

### Evidencia (contenedor cloud, Python 3.11, PostgreSQL 16)
- `python3 -m unittest discover -s tests`: **121 pruebas en verde** (89 previas + 32 nuevas en `tests/test_screening.py`): normalización; Jaro-Winkler contra valores conocidos; índice por token/prefijo y mejor variante por entrada; parsers OFAC y ONU con fixtures ficticios (alias, tipos, nacionalidad, filas rechazadas); rechazo XXE/DOCTYPE, XML mal formado, vacío, tamaño y extensión; descarga desactivada por defecto, solo URLs fijas HTTPS, sin redirecciones y con límite; solo admin carga; versión/hash/cantidad y desactivación de la anterior; auditoría de carga; solo admin/oficial ejecuta; coincidencias explicables e idempotencia; aislamiento por tenant (otra licencia no lista, no obtiene, no decide ni ve historial); fundamento obligatorio y roles (analista y auditor no deciden); confirmación vincula caso y audita; historial append-only; señal en el DataFrame.
- Rendimiento con datos sintéticos: 15.000 entradas con 2 alias cada una (45.000 variantes): construcción del índice 0.43 s; 250 consultas 0.45 s (unas 550 consultas/s). En la prueba de humo real: 130 nombres del Excel de ejemplo en 0.009 s.
- Prueba de humo (`apptest_smoke.py` y un guion extendido con `AppTest`): vistas "Listas de Sanciones" (admin y auditor), "Análisis por Cliente", "Casos de Alerta", "Resumen Ejecutivo", "Manual de Usuario" y sin datos: 0 excepciones y sin XSS. Flujo completo en la UI: carga de lista ficticia, clic en "Ejecutar screening" (4 coincidencias), decisión "Confirmada" con fundamento y verificación de `caso_id` vinculado; el auditor ve el aviso de solo lectura.
- Migración validada con `psql`: (a) DDL del ORM + `001` a `005` dos veces seguidas sin error y sin índices duplicados (14 índices esperados en las cuatro tablas); (b) `001`, `004` y `005` dos veces sin ORM. Con datos de prueba, el trigger rechazó `UPDATE` y `DELETE` en `ScreeningDecisiones` y el CHECK rechazó un estado inválido.
- Duplicación: 10.270 líneas, 180 duplicadas, **1.75 %** (umbral 10 %).

### Variables nuevas
- `SCREENING_AUTO_DOWNLOAD` (opcional, por defecto `false`): habilita la descarga desde las URLs oficiales fijas. La carga manual funciona siempre.

### Pasos de despliegue
1. `scripts/db_migrate.py` aplica `005_screening.sql` al arrancar (ORM primero, SQL después).
2. Un Administrador descarga `sdn.csv` y `alt.csv` (OFAC) y `consolidated.xml` (ONU) desde los sitios oficiales y los carga en Investigación > Listas de Sanciones. Opcional: `SCREENING_AUTO_DOWNLOAD=true` si la red de Railway alcanza `www.treasury.gov` y `scsanctions.un.org`.
3. Ejecutar el screening tras cada carga de análisis y revisar la bandeja.

### Pendientes y motivo
- Los archivos reales de OFAC y ONU no pudieron descargarse (sin red en ambos entornos): los parsers siguen el formato documentado y se probaron con fixtures ficticios. Opciones: validar con los archivos reales en el primer despliegue; si la ONU cambia etiquetas, ajustar `parsear_onu_consolidada`.
- No hay programación automática (cron) de la descarga: la constante y la variable dejan lista la función `descargar_oficial`; se puede invocar desde un servicio cron de Railway (T7 documentará el patrón) o desde la UI.
- La comparación no usa fecha de nacimiento ni documento para reducir falsos positivos (el Excel de entrada no los trae); el fundamento de la decisión exige documentar esa verificación manual.

---

## T4 Bitácora a prueba de alteraciones

**Estado:** HECHO · **Fecha:** 17/09/2026 · **Commit:** `a959f08` (código) y el commit de este informe.

### Qué se hizo
- **Migración `migrations/006_bitacora_hash.sql`** (idempotente, validada dos veces):
  - Columnas nuevas en `public."BitacoraAuditoria"`: `seq BIGINT` (correlativo por `licenciaid`), `hash_prev VARCHAR(64)`, `hash VARCHAR(64)` y `hash_alg VARCHAR(16)` (`sha256` | `hmac-sha256`). Índice único `uq_bitacora_licencia_seq (licenciaid, seq)`; el nombre coincide con la `UniqueConstraint` del ORM, por lo que la ruta real de `db_migrate.py` (ORM primero, SQL después) no duplica índices.
  - `trg_bitacora_inmutable`: rechaza `UPDATE` y `DELETE` sobre cualquier fila (append-only, Art. 19 Ley 6593). En el ORM, listeners `before_update`/`before_delete` lanzan `HistorialInmutableError` (efectivos también en SQLite).
  - `trg_bitacora_validar_eslabon` (BEFORE INSERT): exige `seq`, `hash_prev`, `hash` y `hash_alg`; hash en hexadecimal de 64; primer eslabón de la licencia con `seq = 1` y `hash_prev` génesis (64 ceros); los siguientes con `seq = último + 1` y `hash_prev = hash` del último. La base valida la estructura pero **no calcula el hash**: eso ocurre en un solo punto de la aplicación.
  - **Registros existentes**: quedan como "pre-cadena" (`seq`/`hash` NULL). Decisión y justificación: sellarlos en un bloque génesis exigiría replicar la serialización canónica en PL/pgSQL (dos puntos de cálculo, contrario al requisito) y su integridad anterior a la migración no es demostrable. Desde la migración quedan congelados: el trigger append-only impide alterarlos o borrarlos y el trigger de inserción impide añadir nuevos registros sin cadena, por lo que el conjunto pre-cadena no puede crecer ni cambiar. La verificación los reporta como informativos.
- **Cálculo del hash en un solo punto** (`backend/auditoria.py`):
  - `serializacion_canonica`: JSON con claves ordenadas y sin espacios de `{v: 1, licenciaid, seq, hash_prev, timestamp ISO 8601 UTC con microsegundos, usuario, modulo, accion}`. `timestamp_canonico` convierte a UTC y asume UTC cuando el datetime llega sin zona (como se lee de una columna `TIMESTAMP`).
  - `calcular_hash`: SHA-256, o HMAC-SHA256 cuando `AUDIT_HMAC_KEY` está definida (`hash_alg = hmac-sha256`). Biblioteca estándar (`hashlib`, `hmac`, `json`).
  - `registrar_evento`: en PostgreSQL ejecuta `SET LOCAL TIME ZONE 'UTC'` (garantiza que el valor almacenado, sea la columna `TIMESTAMP` creada por el ORM o `TIMESTAMPTZ` creada por 001, coincida con el timestamp hasheado) y `pg_advisory_xact_lock(clave)` por licencia (clave: 64 bits derivados del UUID) dentro de la transacción del insert; en otros motores (SQLite en pruebas) usa un cerrojo de proceso por licencia. En ambos casos el índice único impide bifurcaciones y ante `IntegrityError` se reintenta hasta 3 veces releyendo el último eslabón. Cualquier otro fallo se registra con `logger.exception`, se cuenta en `fallos_registro` y devuelve `False` sin romper la UI (comportamiento previo conservado). La firma pública de `registrar_evento`/`registrar_evento_autonomo` no cambió: `auth_api.py`, `casos_alerta.py`, `screening_repo.py` y `mod_sesion.py` siguen funcionando sin modificaciones.
  - **HMAC, trade-off y comportamiento sin clave**: sin clave la cadena detecta alteraciones, borrados e inserciones que no recalculen los hashes; un atacante con acceso de escritura a la base que además logre desactivar los triggers podría recalcular toda la cadena. Con `AUDIT_HMAC_KEY` necesita también el secreto de la aplicación. La verificación acepta eslabones `sha256` anteriores al primer eslabón `hmac-sha256` (rotación sin romper el historial), pero marca como ruptura cualquier retroceso a `sha256` después de un eslabón HMAC (síntoma de recálculo sin clave). Si la clave se retira, los eslabones HMAC pasan a "no verificables" y la cadena se reporta rota en el primero de ellos con un motivo explícito.
- **Verificación** `verificar_cadena(db, licenciaid)`: devuelve `ResultadoVerificacion` con `integra`, `total_eslabones`, `verificados`, `pre_cadena`, `primer_seq_roto`, `motivo` ("Hueco en la secuencia: se esperaba seq 3 y se encontró 4 (posible borrado)", "hash_prev no coincide con el hash del eslabón anterior (inserción fuera de orden o alteración)", "El hash no corresponde al contenido del registro (campo alterado)", "Retroceso de HMAC a SHA-256...", "... AUDIT_HMAC_KEY no está configurada"), `hmac_configurado`, `ultimo_seq`, `ultimo_hash` y `detalle` por eslabón (OK / ROTO / NO_VERIFICABLE / NO_VERIFICADO tras la ruptura). Recorre la cadena con `yield_per(500)` y siempre filtrada por `licenciaid`.
- **UI** (`frontend/mod_integridad.py`, vista **Administración > Integridad de Bitácora**): permiso nuevo `verificar_bitacora` (admin y auditor); la vista se oculta del menú a los demás roles y, si se fuerza, muestra el aviso de solo lectura. Panel que indica si HMAC está activo o recomienda configurarlo; botón "Verificar cadena ahora"; KPIs (resultado, eslabones verificados, pre-cadena, primer seq roto), resumen tabular, detalle (eslabones rotos más los 200 más recientes) y **exportación CSV** del reporte completo mediante `exportacion.csv_bytes` (saneado anti fórmulas) con `boton_descarga` (la descarga queda auditada). Cada verificación registra `VERIFICACION_BITACORA:INTEGRA|ROTA:<seq>` en la bitácora como evidencia de la revisión. No requiere análisis cargado.
- Manual de usuario (sección 5.4) y README (variable nueva) actualizados.

### Evidencia (contenedor cloud, Python 3.11, PostgreSQL 16)
- `python3 -m unittest discover -s tests`: **143 pruebas en verde** (121 previas + 22 nuevas en `tests/test_bitacora_integridad.py`): cadena válida (correlativo, génesis, enlaces, detalle); licencia sin eventos; licenciaid inválido; serialización canónica y hash reproducible (claves ordenadas, timestamp `2026-09-17T12:30:45.123456Z`, datetime sin zona asumido UTC, algoritmos inválidos rechazados); resumen para exportación; alteración de `accion` (roto en seq 3, verificados 2, estados OK/OK/ROTO/NO_VERIFICADO/NO_VERIFICADO); alteración de `timestamp`; borrado (hueco detectado en seq 4); borrado del último eslabón (se documenta que solo se detecta comparando el último hash entre verificaciones); inserción fuera de orden (hash_prev incorrecto); seq repetido rechazado por unicidad; registros pre-cadena contados; ORM rechaza UPDATE y DELETE; aislamiento por tenant (romper la licencia B no afecta a A, correlativos independientes); concurrencia simulada (6 hilos × 10 eventos: 60 eslabones sin bifurcación ni fallos); colisión de seq reintentada; error de BD no rompe el flujo y se cuenta; bloqueo advisory y `SET LOCAL TIME ZONE` emitidos cuando el dialecto es PostgreSQL; HMAC: firma con clave, alteración detectada, clave incorrecta detectada en seq 1, sin clave los eslabones HMAC no son verificables, rotación sha256 a HMAC válida y retroceso detectado.
- Migración con `psql`, ruta (a) base legada: `001`, `002`, `004`, `005`, dos registros previos insertados, luego `006` **dos veces** sin error (segunda vez solo avisos "already exists, skipping"). Con datos: `UPDATE` y `DELETE` rechazados tanto en registros pre-cadena como en eslabones; `INSERT` sin cadena rechazado; tres eslabones calculados por `backend/auditoria.py` e insertados por `psql` aceptados; rechazados `seq 5` fuera de orden ("se esperaba 4"), `hash_prev` incorrecto, `hash_alg = md5` y un primer eslabón de otra licencia con `seq 2`; leyendo con la sesión en `America/Guatemala` el timestamp canónico coincide con el hasheado.
- Ruta (b): DDL del ORM (dialecto PostgreSQL) y luego `001` a `006` **dos veces**: sin error, 5 índices en `BitacoraAuditoria` sin duplicados (la restricción única del ORM y el `CREATE UNIQUE INDEX IF NOT EXISTS` de la migración comparten nombre), dos triggers presentes. Comprobado que con `SET LOCAL TIME ZONE 'UTC'` un valor `timestamptz` se guarda como UTC en la columna `TIMESTAMP` sin zona creada por el ORM, aun con la sesión en otra zona.
- Prueba de humo `apptest_smoke.py` en "Integridad de Bitácora", "SIN_DATOS", "Configuración", "Manual de Usuario", "Listas de Sanciones", "Resumen Ejecutivo" y "Casos de Alerta": 0 excepciones, 0 errores, sin XSS. Guion adicional con `AppTest`: admin y auditor ven el botón, verifican (mensaje "Cadena íntegra: N eslabones verificados sin alteraciones"), tabla de detalle y botón de descarga presentes; analista no ve la vista en el menú y recibe el aviso de solo lectura; la cadena queda íntegra tras los eventos generados por la propia UI (10 eslabones incluidos `VERIFICACION_BITACORA:INTEGRA`).
- Duplicación: 10.665 líneas, 180 duplicadas, **1.69 %** (umbral 10 %). Sin `except: pass`; SQL siempre parametrizado (`text()` con binds).

### Variables nuevas
- `AUDIT_HMAC_KEY` (opcional, recomendada en producción): clave para HMAC-SHA256 de los eslabones. Sin ella se usa SHA-256. Una vez definida no debe cambiarse ni retirarse (los eslabones HMAC dejarían de ser verificables); si hubiera que rotarla, planificar un procedimiento de "reverificación y sellado" antes del cambio.

### Pasos de despliegue
1. Definir `AUDIT_HMAC_KEY` en Railway (valor aleatorio de al menos 32 caracteres) **antes** del despliegue, para que el primer eslabón ya sea HMAC. Si se define después, los eslabones anteriores quedan en SHA-256 y siguen verificándose.
2. `scripts/db_migrate.py` aplica `006_bitacora_hash.sql` al arrancar. Importante: el código y la migración deben desplegarse juntos (ya ocurre con el runner al arranque), porque tras la migración la base rechaza inserciones sin cadena.
3. Tras el despliegue, un Administrador o Auditor ejecuta "Verificar cadena ahora" y conserva el reporte CSV (último hash) como línea base para comparaciones futuras.

### Pendientes y motivo
- **Borrado de la cola de la cadena**: eliminar los últimos eslabones (sin dejar hueco) no lo detecta la verificación por sí sola; los triggers lo impiden a nivel de base y el procedimiento documentado (guardar el último hash de cada verificación) lo evidencia. Opción futura: anclar periódicamente el último hash fuera de la base (archivo firmado, servicio externo o correo al auditor) desde un servicio cron (T7 documentará el patrón).
- **Sellado de registros pre-cadena**: no se sellaron (ver justificación). Opción si se desea: procedimiento manual del DBA que exporte y firme el conjunto pre-cadena junto con el primer reporte de verificación.
- **Purga por retención**: la bitácora no admite borrado; una purga posterior a los 5 años exigiría deshabilitar temporalmente los triggers por procedimiento del DBA y, al ser una cadena, solo podrían purgarse eslabones iniciales completos dejando la verificación a partir de un nuevo punto de anclaje (funcionalidad no incluida).
- La verificación por la API FastAPI no se expuso (solo UI Streamlit); si se requiere para auditoría externa, añadir un endpoint `GET /auditoria/integridad` con `require_role(admin, auditor)`.

---

## T5 Cabeceras de seguridad

**Estado:** HECHO (validado de extremo a extremo en el contenedor con el binario real de Caddy; la construcción de la imagen Docker y el primer despliegue en Railway quedan por observar) · **Fecha:** 17/09/2026 · **Commit:** `15baaba` (código) y el commit de este informe.

### Qué se hizo
- **Topología nueva** (`supervisord.conf`, `Dockerfile`, `Caddyfile`): Internet -> borde de Railway (termina TLS) -> **Caddy en `$PORT`** -> Streamlit en `127.0.0.1:8501` -> FastAPI en `127.0.0.1:8000`. Streamlit deja de escuchar en `0.0.0.0`. `PORT` por defecto pasa de 8501 a 8080 (Railway lo inyecta igualmente); `EXPOSE 8080`; `railway.toml` no cambia (`healthcheckPath = /_stcore/health` ahora se sirve a través del proxy).
- **Caddy en la imagen**: en el stage `builder` se descarga `caddy_2.10.2_linux_<arch>.tar.gz` desde la release oficial de GitHub y se verifica con la suma **SHA-512** publicada en `caddy_2.10.2_checksums.txt` (el proyecto Caddy publica SHA-512, no SHA-256; se fijan las sumas de amd64 y arm64 como `ARG`). El binario se copia al stage `runtime` y corre como `appuser`. `XDG_DATA_HOME`/`XDG_CONFIG_HOME` apuntan a `/tmp/caddy` (con `auto_https off` y `admin off` Caddy apenas escribe).
- **Caddyfile** (cada directiva y cada valor de CSP documentados en el propio archivo):
  - `auto_https off`, `admin off`, `encode zstd gzip`, `request_body max_size 26MiB` (Streamlit limita a 25 MiB; en Caddy `MB` es decimal y `MiB` binario, se usa la misma unidad que Streamlit con 1 MiB de margen para el sobre multipart).
  - **CSP**: `default-src 'self'; script-src 'self' 'unsafe-inline' {$CSP_SCRIPT_EXTRA}; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' data: https://fonts.gstatic.com; img-src 'self' data: blob: https://fonts.gstatic.com; connect-src 'self' ws://<host> wss://<host>; worker-src 'self' blob:; frame-src 'self'; frame-ancestors 'none'; object-src 'none'; base-uri 'self'; form-action 'self'; manifest-src 'self'; media-src 'self' blob:`. Justificación resumida: `index.html` de Streamlit incluye un script inline (`window.prerenderReady`) y `components.html` de `app.py` inyecta scripts inline en iframes `srcdoc` que heredan la política, por eso `'unsafe-inline'` en scripts; Emotion y la app inyectan estilos inline; `styles.css` importa Manrope e IBM Plex Mono desde Google Fonts; logotipo y gráficos matplotlib viajan como `data:`; Streamlit y Plotly crean workers `blob:`; Streamlit carga SVG de Material Symbols desde `fonts.gstatic.com`. **No se concede `'unsafe-eval'`**: se inspeccionaron los bundles de Streamlit 1.55 y los únicos usos de `Function(...)` son polyfills de `globalThis` (con fallback) y el motor regl de Plotly, que solo se activa con trazas WebGL; la app usa `Scatter`, `Bar`, `Pie`, `Scatterpolar` e `Indicator` (SVG). `{$CSP_SCRIPT_EXTRA}` es una válvula de escape sin reconstruir la imagen.
  - `Strict-Transport-Security: max-age=31536000` (un año, sin `preload` para que sea reversible, sin `includeSubDomains` porque no se controlan otros subdominios), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=(), interest-cohort=()`. Se eliminan `Server` (Caddy y Tornado) y `Via`. Las operaciones son `defer` para sobrescribir las cabeceras de Streamlit.
  - **IP real sin falsificación**: el borde de Railway envía la IP del cliente en `X-Real-IP` (documentación oficial de Railway, "Request Headers"). Caddy solo la toma en cuenta cuando la conexión procede de un proxy de confianza (`trusted_proxies static private_ranges`, `client_ip_headers X-Real-IP`) y reenvía `{client_ip}` como **valor único** en `X-Forwarded-For` y `X-Real-IP`; cualquier `X-Forwarded-For` enviado por el cliente se sobrescribe. `app.py` (`_cabeceras_origen`) y `backend/rate_limit.py` (`ip_cliente`, primer elemento de la lista, confiado solo desde loopback) siguen funcionando sin cambios y ahora reciben siempre un valor limpio. `X-Forwarded-Proto: https` de Railway se conserva.
- **supervisord**: `[program:caddy]` con prioridad 30 (tras fastapi 10 y streamlit 20), `autorestart=true`, logs a stdout/stderr. Streamlit: `--server.port 8501 --server.address 127.0.0.1`.
- **Herramientas**: `scripts/verificar_cabeceras.py <url> [--websocket]` (solo biblioteca estándar) comprueba página, cabeceras, directivas críticas de CSP, ausencia de `Server`, `/_stcore/health` y el handshake 101 de `/_stcore/stream`; sirve para validar el despliegue real. `tests/test_caddyfile.py` (17 pruebas) parsea el Caddyfile con un parser propio y verifica todas las cabeceras y valores, la CSP (directivas críticas, sin `unsafe-eval`, hosts externos de `styles.css` permitidos), el proxy y las cabeceras de IP, el tope de cuerpo coherente con `config.toml`, la ausencia de TLS automático, y la coherencia de `supervisord.conf` (Streamlit solo loopback, prioridades) y `Dockerfile` (sumas SHA-512 de 128 hex, healthcheck por `$PORT`); ejecuta `caddy validate` si el binario está en PATH.
- README: arquitectura con tres procesos, fila A05 de OWASP y variable `CSP_SCRIPT_EXTRA`.

### Evidencia (contenedor cloud, Python 3.11, Caddy 2.10.2 real)
- Descarga del binario oficial desde GitHub y verificación de la suma SHA-512 contra `caddy_2.10.2_checksums.txt`: `caddy.tar.gz: OK`; `caddy version` = `v2.10.2`. `caddy validate` sobre el Caddyfile: `Valid configuration`.
- Levantados FastAPI (8000), Streamlit (127.0.0.1:8501) y Caddy (8080) con la configuración versionada:
  - `GET /` vía Caddy: 200, `index.html` de Streamlit, `Content-Encoding: gzip`; `index.RuhrnD1v.js` pasa de 2.132.747 a 512.062 bytes.
  - Cabeceras recibidas: CSP completa (con `connect-src 'self' ws://127.0.0.1:8080 wss://127.0.0.1:8080` resuelto desde el host de la petición), HSTS `max-age=31536000`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy`; sin `Server` ni `Via`.
  - `/_stcore/health` vía Caddy: 200 `ok` (mismo camino que usará el healthcheck de Railway).
  - WebSocket: handshake crudo a `/_stcore/stream` responde `101 Switching Protocols`; con un cliente Tornado y el token XSRF como subprotocolo se completó una **sesión real de Streamlit a través de Caddy** (`BackMsg.rerun_script` enviado, recibidos `new_session`, `session_status_changed`, `page_config_changed`), idéntico al resultado directo contra 8501.
  - IP real (Caddy con la misma configuración frente a un upstream de eco): desde loopback con `X-Real-IP: 203.0.113.9` y `X-Forwarded-For: 1.1.1.1, 9.9.9.9` falsificado, Streamlit recibe `X-Forwarded-For: 203.0.113.9` y `X-Real-IP: 203.0.113.9`; sin `X-Real-IP` pero con XFF falsificado recibe la IP de socket (`127.0.0.1`); `X-Forwarded-Proto: https` se conserva; la cabecera `Server` del upstream se suprime.
  - Tope de cuerpo: 20 MiB atraviesan el proxy (403 XSRF de Streamlit, esperado sin token); 26,5 MiB los rechaza Streamlit (400) y 27 MiB los corta Caddy (502 al abortar la lectura): ningún cuerpo mayor de 26 MiB llega completo a Streamlit. El propio front-end de Streamlit bloquea archivos mayores de 25 MB antes de subirlos.
  - `CSP_SCRIPT_EXTRA="'unsafe-eval'"` produce `script-src 'self' 'unsafe-inline' 'unsafe-eval'`; sin la variable, `script-src 'self' 'unsafe-inline'`.
  - `scripts/verificar_cabeceras.py http://127.0.0.1:8080 --websocket`: "Todas las comprobaciones se cumplen" (código 0); contra Streamlit directo (8501) reporta las cabeceras ausentes (código 1).
- `python3 -m unittest discover -s tests`: **160 pruebas en verde** (143 previas + 17 nuevas). Prueba de humo `apptest_smoke.py` "Resumen Ejecutivo": 0 excepciones, sin XSS (no se tocó la UI). Duplicación: 1.67 %.

### Variables nuevas
- `CSP_SCRIPT_EXTRA` (opcional, vacía): fuentes extra para `script-src`. Solo usar si el navegador reporta bloqueos de CSP (consola) y documentarlo.
- `PORT`: la sigue fijando Railway; el valor por defecto de la imagen pasa a 8080.

### Pasos de despliegue
1. Al desplegar, Railway construye la imagen: el stage `builder` debe alcanzar `github.com` para descargar Caddy (si la red de build lo bloquea, la construcción falla de forma explícita en la verificación; ver "Riesgos").
2. No hay cambios de variables obligatorias ni de base de datos. El healthcheck de Railway (`/_stcore/health`) pasa por Caddy; si Streamlit tarda en arrancar Caddy devuelve 502 hasta que responda y Railway espera hasta `healthcheckTimeout` (120 s).
3. Tras el despliegue, ejecutar desde cualquier equipo `python scripts/verificar_cabeceras.py https://<dominio> --websocket` y abrir la aplicación con la consola del navegador visible: no debe haber mensajes "Refused to ... Content Security Policy". Probar login (límite de intentos por IP), carga de Excel, gráficos Plotly, exportaciones PDF/CSV y el manual.
4. Opcional: comprobar con `curl -sI https://<dominio>/` que no aparece `Server`.

### Riesgos de despliegue y cómo revertir
- **CSP demasiado estricta en el navegador real**: la validación de CSP se hizo por análisis de los bundles y de la app, no con un navegador contra el proxy (el contenedor de pruebas no es alcanzable desde un navegador). Si algún componente quedara bloqueado: (a) ajuste inmediato sin redeploy de código, añadiendo la fuente o `'unsafe-eval'` en `CSP_SCRIPT_EXTRA` (solo `script-src`); (b) para otras directivas, editar el Caddyfile y redesplegar; (c) reversión total: ver abajo.
- **Puerto privilegiado**: Caddy corre como `appuser`; si Railway asignara un `PORT` < 1024 fallaría al escuchar. Railway asigna puertos altos; de ocurrir, fijar `PORT` a 8080 en las variables del servicio.
- **Red de build**: la descarga de Caddy requiere salida a `github.com` durante el build; una suma que no coincida aborta el build (comportamiento deseado). Para actualizar Caddy: cambiar `CADDY_VERSION` y las dos sumas desde el `checksums.txt` de la release.
- **Cookie XSRF de Streamlit** (`_streamlit_xsrf`) se emite sin `Secure`/`HttpOnly` (comportamiento de Streamlit, no cambiado). Opción futura: reescribirla en Caddy con `header Set-Cookie` y expresión regular, tras probarla en el navegador.
- **Reversión completa** (volver a Streamlit expuesto sin proxy) en un solo commit: en `supervisord.conf` restaurar `--server.port %(ENV_PORT)s --server.address 0.0.0.0` y eliminar `[program:caddy]`; en `Dockerfile` restaurar `PORT=8501`, `EXPOSE 8501` y el healthcheck contra `localhost:8501` (o `git revert 15baaba`, que deja `Caddyfile` fuera de uso sin efectos). No hay migraciones ni datos afectados; Railway conserva el despliegue anterior si el nuevo no supera el healthcheck.

### Pendientes y motivo
- Prueba con navegador real contra el proxy (consola sin errores de CSP): no fue posible desde el contenedor; se cubre en el paso 3 de despliegue con `CSP_SCRIPT_EXTRA` como mitigación inmediata.
- Construcción de la imagen Docker: no hay Docker en los entornos de trabajo; el Dockerfile se revisó estáticamente (pruebas) y el procedimiento de descarga y verificación se ejecutó a mano con los mismos comandos y sumas.
- `Set-Cookie` con `Secure`/`HttpOnly` para la cookie XSRF y `Cross-Origin-Opener-Policy`: fuera de alcance de T5; se pueden añadir en el Caddyfile tras una prueba en navegador.

---

## T6 MFA TOTP (RFC 6238) para admin y oficial

**Estado:** HECHO · **Fecha:** 17/09/2026 · **Commit:** ver `git log` (commit `feat(seguridad): MFA TOTP ...`) y el commit de este informe.

### Qué se hizo
- **`backend/totp.py`** (solo biblioteca estándar: `hmac`, `hashlib`, `struct`, `base64`, `secrets`): HOTP (RFC 4226) y TOTP (RFC 6238) con HMAC-SHA1, 6 dígitos y paso de 30 s, verificados contra los vectores oficiales de ambos RFC; secreto de 160 bits en Base32; URI `otpauth://` y clave en bloques de 4 para ingreso manual; verificación con ventana de +-1 paso que recorre toda la ventana (sin salida temprana) y compara con `hmac.compare_digest`; **anti-replay**: devuelve el contador coincidente y rechaza cualquier contador menor o igual al último aceptado; códigos de recuperación (10, formato `XXXXX-XXXXX`, alfabeto sin caracteres ambiguos) hasheados con **PBKDF2-HMAC-SHA256** (60.000 iteraciones, sal aleatoria por código, formato autodescriptivo) y verificados en tiempo constante.
- **`backend/mfa.py`** + modelo `MfaUsuario` (`backend/models.py`) + **`migrations/007_mfa.sql`** (idempotente; tabla `public."MfaUsuarios"` con `licenciaid` único, `secreto_cifrado`, `activo`, `enrolado_en`, `ultimo_contador`, `codigos_recuperacion` (JSON de hashes), `codigos_restantes`, `sesion_mfa`, trazas; CHECK de no negativos; los nombres de restricciones coinciden con el ORM para que `db_migrate.py` no duplique nada):
  - Secreto cifrado en reposo con **Fernet** (`cryptography`, ya en requirements) y la clave `MFA_ENCRYPTION_KEY`. Sin clave válida (ausente o no Fernet) la aplicación sigue funcionando pero **no permite enrolar** (`MfaError` / HTTP 409) y la vista muestra al administrador cómo generarla; los códigos de recuperación (solo hashes) siguen funcionando para que un usuario enrolado pueda entrar si la clave se perdió.
  - `iniciar_enrolamiento` (secreto nuevo, inactivo hasta confirmar; rechazado si ya hay MFA activo), `confirmar_enrolamiento` (exige un código válido; genera y devuelve los 10 códigos de recuperación **una sola vez**), `verificar` (TOTP o recuperación, un solo uso, actualiza `ultimo_contador` o elimina el hash usado), `restablecer_propio` (con código válido) y `restablecer_por_admin` (solo rol admin, nunca el propio, auditado en la bitácora del administrador y del usuario afectado).
  - **`sesion_autorizada(db, licencia, session_id)`**: único punto que decide si una sesión de `BitacoraSesions` (creada al validar la contraseña) puede usarse: con MFA activo solo la sesión registrada en `sesion_mfa` tras superar el segundo factor. Lo consultan `get_current_user` de la API y `_restaurar_desde_token` de `app.py`, de modo que **ni un JWT firmado ni un token de restauración firmado permiten saltarse el MFA** (probado).
  - Política `MFA_ENFORCE=true` (por defecto `false`): obligatorio para `admin` y `oficial`; `enrolamiento_pendiente` alimenta el bloqueo en API y UI.
  - Auditoría en `BitacoraAuditoria` (módulo "Autenticación"): `MFA_OK`, `MFA_FALLIDO`, `MFA_ENROLADO`, `MFA_RECUPERACION`, `MFA_RESTABLECIDO:propio|admin=<u>|usuario=<u>` (constantes en `backend/auditoria.py`).
- **API (`auth_api.py`, `backend/schemas.py`)**:
  - Todos los JWT llevan `typ` (`acceso` | `mfa`) y `get_current_user` exige `typ = acceso`, sesión vigente y `sesion_autorizada`; con `MFA_ENFORCE` un admin/oficial sin enrolar recibe **403** en todo salvo los endpoints de MFA (`get_usuario_para_mfa`).
  - `POST /auth/validate`: si el usuario tiene MFA activo responde `is_active=false`, `mfa_requerido=true` y `mfa_token` (JWT `typ=mfa`, **5 minutos**, `jti` de **un solo uso** registrado en memoria al canjearse; sin `licencia`, `access_token` ni `session_id`). El token intermedio no sirve en ningún otro endpoint. Sin MFA activo la respuesta es la anterior más `mfa_enrolamiento_pendiente`.
  - `POST /auth/mfa/verificar` (`mfa_token`, `codigo` TOTP o de recuperación): límite de intentos reutilizando `backend/rate_limit.py` (claves `ip:` y `mfa:<usuario>`, 5 fallos / 5 min, bloqueo 15 min, `Retry-After`); un intento fallido no invalida el token (lo acota el límite), un canje exitoso sí. Emite el JWT final y la sesión.
  - `POST /token` (OAuth2 password): campo de formulario opcional `codigo_mfa`; con MFA activo y sin código válido responde 401.
  - `GET /auth/mfa/estado`, `POST /auth/mfa/enrolar` (409 sin clave o con MFA activo), `POST /auth/mfa/confirmar` (devuelve los códigos de recuperación una vez), `POST /auth/mfa/restablecer` (propio, con código), `POST /auth/mfa/restablecer/{licencia_id}` (solo admin, 400 si es el propio, 404 si no existe).
- **UI (Streamlit)**:
  - Login (`app.py`): tras la contraseña, si la API pide MFA se muestra el formulario "Verificación en dos pasos" (código del autenticador o de recuperación, `autocomplete="one-time-code"`, botón Volver); expira a los 5 minutos; los mensajes 401/429 de la API se muestran tal cual. La sesión no se marca autenticada hasta canjear el código.
  - Nueva vista **Administración > Seguridad de la Cuenta** (`frontend/mod_mfa.py`, no requiere análisis cargado): KPIs de estado, política y códigos restantes; enrolamiento con **código QR si la librería opcional `qrcode` está instalada** (añadida a `requirements.txt` como `qrcode==8.2`; no existe en el contenedor de pruebas, por lo que allí y en cualquier despliegue sin ella se muestra el panel "Ingreso manual" con la clave en bloques, la cuenta, el emisor y la URI `otpauth` en un desplegable); confirmación con un código antes de activar; códigos de recuperación mostrados una sola vez con descarga `.txt` y botón "He guardado mis códigos"; restablecimiento propio con código; sección de administrador para restablecer el MFA de otro usuario por nombre (casilla de confirmación de identidad; el propio se rechaza). Permiso nuevo `restablecer_mfa_usuarios` (admin) en `frontend/permisos.py`.
  - **Gate de la política**: con `MFA_ENFORCE=true`, un admin/oficial sin enrolar solo ve la pantalla "Enrolamiento obligatorio del segundo factor" (sin barra lateral ni vistas) con el botón Cerrar sesión; el resultado se memoriza por `session_id` y se libera cuando el usuario confirma haber guardado sus códigos.
  - `_restaurar_desde_token` valida `mfa.sesion_autorizada` (prueba estática y funcional). Al cerrar sesión se limpian las claves `mfa_*` del estado.
  - Manual de usuario (sección 5.5) y README (módulos, fila A07, variables) actualizados.

### Evidencia (contenedor cloud, Python 3.11, PostgreSQL 16)
- `python3 -m unittest discover -s tests`: **189 pruebas en verde** (160 previas + 29 nuevas en `tests/test_mfa.py`): vectores HOTP RFC 4226 (10) y TOTP RFC 6238 SHA-1 (6, con 8 y 6 dígitos); secreto Base32 de 160 bits; URI otpauth; ventana +-1 (acepta -1/0/+1, rechaza +-2); anti-replay por contador; formatos inválidos; hash y verificación de códigos de recuperación (formato, sal, tiempo constante, hashes malformados); secreto cifrado en BD distinto del claro y no descifrable con otra clave; sin clave no se enrola y el estado lo informa (clave ausente e inválida); confirmación exige código válido y audita `MFA_FALLIDO`/`MFA_ENROLADO`; replay del mismo código rechazado; recuperación de un solo uso (10 -> 9, reutilización rechazada, mayúsculas/minúsculas); recuperación funciona sin clave de cifrado; `sesion_autorizada` solo para la sesión que superó el MFA; **restauración de sesión firmada** (token `session_token` válido sobre una sesión creada solo por contraseña) no autoriza hasta verificar el código; `app.py` llama a `sesion_autorizada` al restaurar; política enforce por rol; restablecimiento propio (código erróneo rechazado, propio por admin rechazado, no admin rechazado, admin distinto correcto) con auditoría cruzada. API con `TestClient`: login sin MFA inalterado; flujo completo (token intermedio sin acceso a `/perfil` ni `/auth/mfa/estado`, código erróneo 401, correcto 200 con licencia y sesión, `MFA_OK` y `MFA_FALLIDO` auditados); token intermedio de un solo uso, `typ=mfa`, vigencia <= 5 min, expirado rechazado, JWT de acceso no canjeable; JWT de acceso legítimo sobre sesión sin MFA rechazado (401); límite de intentos (6.º intento 429 con `Retry-After`); `/token` exige `codigo_mfa` (TOTP y recuperación); enrolamiento por API (estado, enrolar, confirmar erróneo 400, confirmar correcto 200 con 10 códigos, re-enrolar 409, recuperación de un solo uso en un login nuevo); enrolar sin clave 409; `MFA_ENFORCE` bloquea `/perfil` (403) hasta confirmar y no afecta al analista; restablecer por API (propio por id 400, inexistente 404, otro usuario 204, analista 403, propio con código 204).
- Migración con `psql`: ruta (a) DDL del ORM (dialecto PostgreSQL) y `001` a `007` **dos veces** sin error, tabla con `uq_mfa_licencia` y los dos CHECK sin duplicados; ruta (b) `001` y `007` dos veces sin ORM; el CHECK rechazó `codigos_restantes = -1` y la restricción única rechazó un segundo registro para la misma licencia.
- Prueba de humo `apptest_smoke.py` en "LOGIN", "Seguridad de la Cuenta", "SIN_DATOS" y "Resumen Ejecutivo": 0 excepciones, sin XSS. Guion extendido con `AppTest` y usuarios reales en SQLite: enrolamiento completo en la UI (Iniciar -> clave manual y URI otpauth visibles -> código erróneo rechazado -> código correcto -> 10 códigos mostrados una vez -> "He guardado" -> estado Activo); admin restablece a oficial y no puede restablecerse a sí mismo; restablecimiento propio con código de recuperación; oficial y analista no ven la sección de administrador; con `MFA_ENFORCE=true` el oficial sin enrolar solo ve la pantalla de enrolamiento sin barra lateral, enrola y recupera la aplicación normal, el analista no se ve afectado, el botón Cerrar sesión del gate funciona; sin clave el admin ve el aviso con la variable y no hay botón de enrolar; formulario de dos pasos en el login (sin campo de contraseña, botón Volver) con el nombre de usuario escapado.
- Duplicación: 11.776 líneas, 180 duplicadas, **1.53 %** (umbral 10 %). Sin `except: pass`; SQL solo por ORM.

### Variables nuevas
- `MFA_ENCRYPTION_KEY` (opcional; obligatoria para habilitar MFA): clave Fernet. Generar con `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`. **No cambiarla ni retirarla** con usuarios enrolados: sus secretos dejarían de descifrarse (solo entrarían con códigos de recuperación y un administrador tendría que restablecer su MFA).
- `MFA_ENFORCE` (opcional, por defecto `false`): `true` exige MFA a `admin` y `oficial`.

### Orden de activación seguro en producción
1. Desplegar el código: `scripts/db_migrate.py` aplica `007_mfa.sql` al arrancar. Sin variables nuevas nada cambia para los usuarios (login idéntico; `/auth/validate` devuelve los campos nuevos con valores neutros).
2. Definir `MFA_ENCRYPTION_KEY` en Railway (guardar copia en el gestor de secretos de la organización) y redesplegar. `MFA_ENFORCE` sigue en `false`.
3. Cada Administrador y Oficial entra en **Administración > Seguridad de la Cuenta**, enrola su autenticador y guarda sus códigos de recuperación. Comprobar que al menos **dos administradores** están enrolados (para poder restablecer el MFA de otro sin quedar bloqueados).
4. Verificar un cierre y reinicio de sesión con código (y una recarga del navegador: la restauración de sesión debe seguir funcionando tras el MFA).
5. Poner `MFA_ENFORCE=true` y redesplegar: los admin/oficial que aún no enrolaron verán solo la pantalla de enrolamiento en su próximo login; analistas y auditores no se ven afectados.
6. Si la clave se perdiera: los usuarios enrolados pueden entrar con códigos de recuperación; un administrador restablece el MFA de los demás y cada uno vuelve a enrolar con la clave nueva. Documentar el incidente en la bitácora (queda `MFA_RECUPERACION` y `MFA_RESTABLECIDO`).

### Pendientes y decisiones
- **Código QR**: `qrcode` no pudo instalarse ni probarse en el contenedor (PyPI bloqueado); se añadió a `requirements.txt` (pura Python, usa Pillow ya presente) y la UI degrada a la clave manual si falta. Si el build de Railway fallara por esa línea, retirarla del `requirements.txt`: el MFA funciona igual con ingreso manual.
- **Registro de `jti` canjeados y límite de intentos en memoria** (un solo worker, como el limitador previo): con varios workers migrar a Redis. El resto del estado MFA (contador, sesión verificada, códigos) vive en la base de datos.
- **Sesiones abiertas al restablecer**: un administrador que restablece el MFA de un usuario con sesión Streamlit abierta no la cierra; el gate se reevalúa en el próximo login. Opción futura: invalidar la sesión (`BitacoraSesions`) al restablecer.
- **Colección Postman** no actualizada con los endpoints `/auth/mfa/*` (documentados en este informe y en OpenAPI `/docs`).
- El endpoint OAuth2 `/token` acepta el código en el mismo formulario (`codigo_mfa`) porque el flujo password no tiene segundo paso; los clientes de API deben preferir `/auth/validate` + `/auth/mfa/verificar`.

---

## T7 Respaldos

**Estado:** HECHO (servicio cron en Railway documentado, no ejecutado; destino S3 implementado pero no probado por falta de boto3) · **Fecha:** 17/09/2026 · **Commit:** `3e9be0f` (código) y el commit de este informe.

### Qué se hizo
- **Módulo `backend/respaldos.py`** (biblioteca estándar + `cryptography`; sin psycopg2, usa `pg_dump`, `pg_restore` y `psql`):
  - **Conexión sin exponer la contraseña**: `DATABASE_URL` (o `DB_HOST/DB_PORT/DB_USER/DB_PASS/DB_NAME`, como `backend/database.py`) se descompone con `urllib.parse`; la contraseña se entrega a los procesos hijo solo mediante `PGPASSWORD` en su entorno (nunca en la línea de comandos, nunca exportada al proceso padre). Se respeta `sslmode` de la URL. Toda salida pasa por `url_sin_credenciales` y por `FiltroSinSecretos`, un filtro de logging que enmascara las URL `://usuario:clave@` y la contraseña literal (de 6 o más caracteres) en cualquier mensaje, incluidos los errores de `pg_dump`/`psql`.
  - **Cifrado en streaming AES-256-GCM por bloques de 4 MiB** en lugar de Fernet. Justificación: Fernet carga el archivo completo en memoria y lo expande un 33 % (base64); un volcado de producción puede pesar cientos de MB y el cron de Railway corre con memoria acotada. La clave maestra es `BACKUP_ENCRYPTION_KEY` en formato Fernet (32 bytes base64 url safe, la misma forma que `MFA_ENCRYPTION_KEY`) y de ella se derivan por HKDF-SHA256 una clave de cifrado y otra de firma. Formato: cabecera `SAMLBK01` + prefijo de nonce aleatorio (8 bytes); cada bloque lleva longitud, marca de último bloque y etiqueta GCM; el contador de bloque y la marca forman parte de los datos autenticados, por lo que se detectan bloques alterados, reordenados, archivos truncados o con datos añadidos. Medido: 64 MiB cifrados en 0.15 s y descifrados en 0.09 s.
  - **Manifiesto JSON** `respaldo_AAAAMMDDTHHMMSSZ.manifest.json`: fecha UTC, bytes y SHA-256 del volcado en claro y del archivo cifrado, versión de `pg_dump`, host/puerto/base (sin credenciales), lista de migraciones aplicadas según `schema_migrations` y `ultima_migracion`, y **anclas de auditoría**: último `seq`, `hash` y `hash_alg` de la cadena de `BitacoraAuditoria` de cada licencia (T4), leídos con `psql`. El manifiesto se **firma con HMAC-SHA256**; un manifiesto editado (por ejemplo, con un hash "corregido") se rechaza.
  - **Almacenes**: `AlmacenLocal` (`BACKUP_DIR`, archivos con permisos 0600 y escritura atómica) y `AlmacenS3` (Railway Buckets o cualquier S3 compatible) que se activa con `BACKUP_S3_BUCKET` solo si `boto3` está instalado; si no lo está, error explícito que indica añadirlo o usar `BACKUP_DIR`.
  - **Retención abuelo-padre-hijo** (`PoliticaRetencion`, por defecto 7 diarios, 4 semanales ISO, 12 mensuales, configurable): se conservan los N más recientes, el más reciente de cada una de las N últimas semanas y de los N últimos meses. **Nunca se borra el respaldo válido más reciente** aunque la política sea 0/0/0. Un respaldo se considera válido si tiene manifiesto bien formado y el tamaño del archivo coincide; los inválidos se conservan y se reportan (solo se purgan huérfanos sin manifiesto con más de 30 días). Un nombre repetido (dos corridas en el mismo segundo) no se sobrescribe.
- **`scripts/respaldo_bd.py`**: `pg_dump -Fc --no-owner --no-privileges` a un directorio temporal 0700 (`BACKUP_TMPDIR` opcional), cifrado, borrado del volcado en claro, manifiesto firmado, subida y retención. Opciones `--sin-retencion`, `--solo-retencion [--simular]`, `--listar`, `--json` y `--generar-clave`. Código de salida 1 en cualquier error controlado (clave ausente o malformada, base inexistente, `pg_dump` fallido).
- **`scripts/restaurar_bd.py`**: origen `--respaldo <nombre>` (del almacén) o `--archivo <ruta.dump.enc>`; acción `--destino <URL>` (explícita), `--destino-local` (DB_* de `.env`, para `./aml`), `--verificar-solo` o `--solo-descifrar <ruta>`. Flujo: firma del manifiesto -> tamaño y SHA-256 del cifrado -> descifrado autenticado -> SHA-256 y tamaño del volcado -> `pg_restore --exit-on-error` (con `--crear-base` y `--limpiar` opcionales) -> comparación de `schema_migrations` con el manifiesto -> impresión de las anclas de auditoría para cotejarlas con "Integridad de Bitácora". **Protección de producción**: si el destino coincide con `DATABASE_URL` (host, puerto y base) o su host pertenece a `*.railway.internal`, `*.rlwy.net`, `*.railway.app` o `BACKUP_HOSTS_PRODUCCION`, se niega con código 2 salvo que concurran la bandera `--confirmar-produccion` **y** la variable `BACKUP_PERMITIR_RESTAURAR_PRODUCCION=si`; en ese caso muestra una advertencia antes de continuar. La comprobación ocurre antes de tocar archivos o la base.
- **Orquestador `./aml`** (reutiliza `dump`/`restore`): `./aml dump` usa `scripts/respaldo_bd.py` cuando `.env` define `BACKUP_ENCRYPTION_KEY` (excluyendo `DATABASE_URL`, que apunta a producción, para respaldar la base local) y, si no, conserva el `.dump` en claro con un aviso; `./aml restore archivo.dump.enc` delega en `scripts/restaurar_bd.py --destino-local --limpiar` y los `.dump` siguen por la ruta previa; nuevo `./aml backups` lista el inventario y su validez.
- **`railway.respaldo.toml`**: configuración del servicio cron (misma imagen, `startCommand = "python scripts/respaldo_bd.py"`, `cronSchedule = "0 3 * * *"`, sin healthcheck).

### Evidencia (contenedor cloud, Python 3.11, PostgreSQL 16.13, pg_dump 16.13)
- `python3 -m unittest discover -s tests`: **218 pruebas en verde** (189 previas + 29 nuevas en `tests/test_respaldos.py`): parseo de URL con `sslmode` y contraseña codificada, `repr`/`str` sin contraseña, `PGPASSWORD` solo en el entorno del hijo, URL inválidas; claves inválidas; cifrado ida y vuelta multibloque, archivo vacío, clave incorrecta, byte alterado, truncado, datos añadidos, cabecera ajena y bloques reordenados; firma del manifiesto (alteración, otra clave, sin firma), estructura, verificación completa con hash de cifrado y de volcado distintos; retención (400 días de respaldos: 7 diarios, semanas ISO y 12 meses distintos, cota 23), último válido conservado con política 0/0/0, política desde entorno, borrado real con almacén en memoria (antiguos borrados, inválidos y huérfanos recientes conservados, huérfano viejo purgado, `--simular` sin cambios), permisos 0600 del almacén local, S3 sin boto3; detección de producción (misma base, otra base del mismo host, hosts Railway y proxy, hosts extra) y doble confirmación (ninguna, solo bandera, solo variable, ambas); filtro de logging y `_salida_segura`; scripts: respaldo sin clave no expone la contraseña, restauración bloqueada por producción sin invocar ningún ejecutable, nombre `../etc/passwd` y archivo inexistente rechazados, `--verificar-solo` y `--solo-descifrar` sobre archivo local (0600, no sobrescribe).
- **Prueba real** con dos bases: `aml_origen` creada con el DDL del ORM más `001` a `007` y `schema_migrations` (7 filas), 2 licencias, 4 eslabones de bitácora (dos licencias, con los triggers de T4 activos) y 50 casos de alerta. `scripts/respaldo_bd.py` con `DATABASE_URL=postgresql://postgres:***@localhost/aml_origen`: volcado 65 399 bytes, cifrado 65 436, manifiesto con 7 migraciones y 2 anclas (`seq 3` y `seq 1`); la salida y el manifiesto no contienen la contraseña (`grep` de `:pw@` y `PGPASSWORD` = 0). `scripts/restaurar_bd.py --destino .../aml_destino --crear-base`: base creada, restaurada, "7 migraciones coinciden". Comparación: conteos iguales (2/4/50/7), 26 tablas en ambas, 5 triggers, y **md5 del contenido ordenado de `Licencias`, `BitacoraAuditoria`, `CasosAlerta` y `schema_migrations` idénticos** entre origen y destino.
- Casos negativos reales: destino igual a `DATABASE_URL` sin confirmación -> código 2; solo bandera -> 2; host `postgres.railway.internal` -> 2; ambas confirmaciones -> advertencia y restauración con `--clean` completada. Un byte alterado en el cifrado -> "SHA-256 del archivo cifrado no coincide"; hash editado en el manifiesto -> "firma del manifiesto no es válida"; archivo truncado -> tamaño distinto; clave incorrecta -> firma inválida (y, sin manifiesto, "No se pudo autenticar el bloque 0"); clave malformada y clave ausente -> mensajes claros, código 1; base inexistente -> código 1 sin contraseña en el mensaje de `psql`.
- `./aml dump`, `./aml backups` y `./aml restore backups/<nombre>.dump.enc` probados en el contenedor con un `.env` local ficticio cuyo `DATABASE_URL` apuntaba a un host de Railway: el respaldo tomó la base local (DB_*) y la restauración no se consideró producción.
- Duplicación: 1.52 % (umbral 10 %).

### Variables nuevas
- `BACKUP_ENCRYPTION_KEY` (obligatoria para respaldar/restaurar): generar con `python scripts/respaldo_bd.py --generar-clave` (o `Fernet.generate_key()`). Guardar copia en el gestor de secretos: **sin ella los respaldos no se pueden descifrar**. Si se rota, los respaldos anteriores requieren la clave antigua (conservarla etiquetada por fecha).
- `BACKUP_DIR` (por defecto `backups/`): destino local (en Railway, un volumen montado, por ejemplo `/data/respaldos`).
- `BACKUP_S3_BUCKET`, `BACKUP_S3_ENDPOINT`, `BACKUP_S3_REGION`, `BACKUP_S3_PREFIX` (por defecto `sovereign-aml`) y las estándar `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`: destino S3 compatible; requiere `boto3`.
- `BACKUP_RETENCION_DIARIOS` / `BACKUP_RETENCION_SEMANALES` / `BACKUP_RETENCION_MENSUALES` (7 / 4 / 12).
- `BACKUP_TMPDIR` (opcional): dónde se escribe el volcado en claro durante segundos; debe ser un sistema de archivos privado.
- `BACKUP_HOSTS_PRODUCCION` (opcional, lista separada por comas): hosts adicionales que se consideran producción al restaurar.
- `BACKUP_PERMITIR_RESTAURAR_PRODUCCION=si` (solo durante una restauración de producción autorizada; retirarla después).

### Servicio cron en Railway (documentado, no ejecutado)
1. **Imagen**: la imagen actual (`python:3.11-slim`) no incluye `pg_dump`; la base de Railway es PostgreSQL 18 y `pg_dump` debe ser de versión igual o superior. Añadir en la etapa `runtime` del `Dockerfile` (pendiente de verificar en un build de Railway):
   ```dockerfile
   RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates curl gnupg \
       && install -d /usr/share/postgresql-common/pgdg \
       && curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc -o /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc \
       && echo "deb [signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc] https://apt.postgresql.org/pub/repos/apt bookworm-pgdg main" > /etc/apt/sources.list.d/pgdg.list \
       && apt-get update && apt-get install -y --no-install-recommends postgresql-client-18 \
       && rm -rf /var/lib/apt/lists/*
   ```
   Si se usa un bucket, añadir `boto3` a `requirements.txt`.
2. **Servicio**: en el proyecto de Railway, "New > GitHub Repo" con el mismo repositorio y rama; en Settings > Config-as-code fijar "Config file path" en `railway.respaldo.toml` (aporta `startCommand` y `cronSchedule = "0 3 * * *"`; los horarios se evalúan en UTC, granularidad mínima 5 minutos y un disparo se omite si el anterior sigue corriendo, por eso el script termina solo). No exponer dominio público.
3. **Variables del servicio cron**: `DATABASE_URL = ${{Postgres.DATABASE_URL}}` (red privada), `BACKUP_ENCRYPTION_KEY` (la misma que se guardó en el gestor de secretos), y **una** de estas opciones de destino:
   - Volumen: montar un volumen en `/data` y `BACKUP_DIR=/data/respaldos`. Sencillo, pero el respaldo vive en la misma región/proyecto que la base.
   - Bucket (recomendado, fuera de la base): crear un Storage Bucket y, desde su pestaña Credentials, referenciar `BACKUP_S3_BUCKET=${{Bucket.BUCKET}}`, `BACKUP_S3_ENDPOINT=${{Bucket.ENDPOINT}}`, `BACKUP_S3_REGION=${{Bucket.REGION}}`, `AWS_ACCESS_KEY_ID=${{Bucket.ACCESS_KEY_ID}}`, `AWS_SECRET_ACCESS_KEY=${{Bucket.SECRET_ACCESS_KEY}}`. Los objetos quedan en `sovereign-aml/respaldo_<fecha>.dump.enc` y `.manifest.json`.
   - Retención opcional con `BACKUP_RETENCION_*`. No definir `MFA_*`, `SECRET_KEY` ni `PORT`: el script no los usa.
4. **Ancla externa de la bitácora**: cada manifiesto registra el último `seq`/`hash` por licencia. Como el bucket o volumen está fuera de la base, constituye la ancla externa recomendada en T4 para detectar el borrado de la cola de la cadena: el auditor compara el `ultimo_hash` del manifiesto más reciente con el resultado de "Administración > Integridad de Bitácora"; si la cadena actual tiene un `seq` menor o un hash distinto para el mismo `seq`, hubo alteración posterior al respaldo. Los manifiestos son pequeños (unos KB) y pueden conservarse indefinidamente aunque la retención borre los volcados.
5. **Verificación periódica**: al menos mensualmente, un operador ejecuta `python scripts/restaurar_bd.py --respaldo <último> --verificar-solo` desde un servicio con las mismas variables (o descarga el par de archivos y lo hace localmente) y, trimestralmente, una restauración completa en una base de ensayo (runbook siguiente).

### Runbook de restauración
Prerrequisitos: `pg_restore`/`psql` de versión igual o superior a la del servidor origen, `BACKUP_ENCRYPTION_KEY` vigente en la fecha del respaldo y acceso al almacén (`BACKUP_DIR` local con el par `.dump.enc` + `.manifest.json`, o variables `BACKUP_S3_*`).
1. **Elegir el respaldo**: `python scripts/respaldo_bd.py --listar` (muestra fecha y validez) y leer su manifiesto: `ultima_migracion` debe ser compatible con la versión del código que se va a desplegar (una migración posterior se aplicará sola al arrancar; una anterior a la del código requiere desplegar el código correspondiente).
2. **Verificar sin restaurar**: `python scripts/restaurar_bd.py --respaldo respaldo_<fecha> --verificar-solo`. Debe imprimir "Verificación correcta". Cualquier error de firma, hash o autenticación de bloque invalida el respaldo: usar el anterior y abrir incidente.
3. **Ensayo en base de prueba** (siempre antes de producción): `python scripts/restaurar_bd.py --respaldo respaldo_<fecha> --destino postgresql://usuario:clave@host:5432/aml_ensayo --crear-base`. Al final imprime "N migraciones coinciden" y las anclas. Comprobar conteos de `Licencias`, `BitacoraAuditoria` y `CasosAlerta`, y ejecutar "Integridad de Bitácora" apuntando la aplicación a `aml_ensayo` (el `ultimo_hash` de cada licencia debe coincidir con el manifiesto).
4. **Restauración en producción** (solo con incidente documentado y dos personas): detener el servicio de la aplicación en Railway (evitar escrituras durante la restauración); exportar `BACKUP_PERMITIR_RESTAURAR_PRODUCCION=si` y `DATABASE_URL` de producción; ejecutar `python scripts/restaurar_bd.py --respaldo respaldo_<fecha> --destino "$DATABASE_URL" --confirmar-produccion --limpiar` (con `--limpiar` los objetos existentes se sustituyen; sin él, `pg_restore` falla si ya existen). Leer la advertencia. Al terminar, retirar la variable, reiniciar la aplicación (aplica migraciones pendientes) y ejecutar "Verificar cadena ahora" en Integridad de Bitácora; conservar el CSV y el manifiesto usado como evidencia. Registrar en la bitácora de operaciones quién, cuándo, respaldo usado y hashes.
5. **Local con `./aml`**: `./aml restore backups/respaldo_<fecha>.dump.enc` restaura sobre la base local de `.env` con `--limpiar`.
6. **Solo obtener el volcado en claro** (por ejemplo, para `pg_restore -l`): `python scripts/restaurar_bd.py --respaldo <nombre> --solo-descifrar /ruta/privada/volcado.dump` (permisos 0600; borrar al terminar).

### Pendientes y motivo
- **Dockerfile y `requirements.txt`**: la instalación de `postgresql-client-18` y de `boto3` no se aplicó porque no es posible construir la imagen ni instalar paquetes en los entornos disponibles (PyPI bloqueado); el snippet exacto queda arriba. Sin `pg_dump` en la imagen, el servicio cron falla con "No se encontró el ejecutable 'pg_dump'". Opción alternativa: imagen separada para el cron basada en `postgres:18` con Python y `cryptography`.
- **`AlmacenS3` no probado** contra un bucket real (sin boto3 ni red). El código sigue la API estándar de boto3 (`upload_file`, `download_file`, `list_objects_v2` paginado, `head_object`, `delete_object`); probar con `--sin-retencion` la primera vez y revisar con `--listar`.
- **`pg_dump` 18 en el equipo local**: `railway_pull.sh` ya busca `postgresql@18`; `scripts/respaldo_bd.py` usa el `pg_dump` del PATH. Si el PATH tiene una versión menor que el servidor, `pg_dump` lo rechaza con mensaje claro; en ese caso anteponer el binario 18 al PATH.
- La rotación de `BACKUP_ENCRYPTION_KEY` no re-cifra respaldos antiguos (requeriría descifrar y volver a cifrar cada uno); procedimiento manual con `--solo-descifrar` si se necesitara.

---

## T8 Detección de anomalías

**Estado:** HECHO · **Fecha:** 17/09/2026 · **Commit:** `30e2cab` (código) y el commit de este informe.

### Qué se hizo
- **Módulo puro `backend/anomalias.py`** (numpy + pandas, sin tocar `backend/procesador.py`):
  - **Variables por transacción**: monto, log monto, desviación frente al perfil declarado, monto relativo a la mediana del propio cliente, monto redondo (múltiplo de 1,000), proximidad a umbrales para fraccionamiento (monto en [80 %, 100 %) de RTE USD 10,000, `umbral_absoluto` y `umbral_feic`), fin de semana, hora atípica (fuera de 08:00-18:00, solo si la fecha trae hora), ubicación de riesgo (`Ubicacion_Riesgo` o `UbicacionRiesgo`) y operación en efectivo (`Tipo_Instrumento` o `TipoOperacion`).
  - **Variables por cliente**: monto máximo, mediana, coeficiente de variación, desviación máxima frente al perfil, total/perfil, número de operaciones, días activos, máximo de operaciones en un día, proporciones de montos redondos, cerca de umbral, fin de semana, hora atípica, ubicación de riesgo y efectivo, contrapartes distintas, contrapartes por operación y concentración en la contraparte principal (`Cliente_Destino`). Las variables cuya columna falta no se crean; las constantes se descartan; nulos y textos no numéricos se tratan como 0.
  - **Modelo elegido**: ensamble de **Isolation Forest implementado con numpy** (psi = 256, altura límite ceil(log2 psi), normalización c(n), s = 2^(-E[h]/c(psi)), 100 árboles, semilla 42, árboles en arreglos y evaluación vectorizada por nivel) y **puntaje robusto MAD multivariable** (media de |x - mediana| / (1.4826 · MAD), recortada en 10), combinados por promedio de percentiles dentro del lote. Justificación: sklearn no está en `requirements.txt` ni en los entornos de prueba (PyPI bloqueado) y añadirlo sumaría más de 100 MB (scipy + scikit-learn) a la imagen; la implementación propia es reproducible y suficiente. El ensamble se adoptó porque en las pruebas con anomalías inyectadas el Isolation Forest solo alcanzaba precisión 0.53-0.67 en el top 15 (se deja llevar por clientes de una sola operación con una combinación rara) y el MAD solo captura magnitud pero no interacciones; juntos alcanzan 0.80 en lotes pequeños y 0.96 en 50,000 transacciones. **Fallback probado**: con menos de 12 clientes (o 12 transacciones para la señal por operación) se usa únicamente el puntaje MAD (`metodo_clientes = mad_multivariable`).
  - **Salida**: `ResultadoAnomalias` con `clientes` (Cliente, Anomalia_Score 0-1, Anomalia_Percentil 0-100, Anomalia_Nivel Alto/Medio/Bajo, Anomalia_Variables, Anomalia_Explicacion), `transacciones` (percentil por operación, índice alineado con el DataFrame), `variables_cliente` y `metadata` (versión `anomalias-if-mad-numpy/1.0.0`, método, parámetros, hash de parámetros, hash del lote, variables usadas, duración).
  - **Explicabilidad**: por cliente, las 3 variables con mayor desviación robusta frente a la cartera, con valor del cliente, referencia (mediana del lote), dirección (por encima / por debajo) y número de desviaciones robustas, redactadas en lenguaje claro ("Máximo de operaciones en un mismo día: 13, por encima de la cartera (mediana 1; 16.2 desviaciones robustas)").
  - `marcar_casos` añade in situ `Anomalia_Percentil` y `Anomalia_Nivel` al DataFrame de casos; `puntos_ciegos` devuelve clientes con anomalía Alta y `Score_Max` menor que el umbral configurado; `resumen_trazabilidad` produce la línea para reportes.
- **Configuración (`backend/config_aml.py`, lista blanca `AmlConfig`)**: `anomalia_activa` (True), `anomalia_percentil_alto` (90, 50-100), `anomalia_percentil_medio` (75, 0-99, validado menor que el alto), `anomalia_n_arboles` (100, 10-500), `anomalia_semilla` (42), `anomalia_score_punto_ciego` (3.0, 0-10). Panel "Señal de anomalía" en Configuración > Reglas de Detección (toggle, percentiles, umbral de punto ciego, árboles, semilla) y fila en el resumen; validación al aplicar.
- **UI (`frontend/mod_anomalias.py`)**:
  - Pestaña **"Señal de Anomalía"** en Imperator Diagnostics: KPIs (clientes con anomalía alta, posibles puntos ciegos, método, tiempo), **gráfico de dispersión Score IMPERATOR vs percentil de anomalía** (plotly con `plotly_dark_layout`, colores por nivel, borde ámbar para puntos ciegos, líneas de referencia), tabla de **posibles puntos ciegos de las reglas** (anomalía alta y score bajo), ranking filtrable por nivel, detalle explicativo por cliente (tabla de 3 variables + lectura para el Oficial + 5 operaciones más atípicas del cliente) y expander de **limitaciones y gobierno del modelo** con la línea de trazabilidad.
  - **Análisis por Cliente**: ficha "Señal de Anomalía (complementaria)" con percentil, nivel, indicador de punto ciego, las 3 variables y trazabilidad.
  - Columnas "Señal de anomalía (percentil)" y "Nivel de anomalía" en Casos de Alerta y Matrices de Riesgo (marcadas en `app.py` junto a la señal de screening).
  - Informe Ejecutivo PDF: párrafo con clientes en anomalía alta, puntos ciegos y **trazabilidad (versión del modelo, método, árboles, muestra, semilla, percentiles, hash de parámetros y de lote)**.
  - **Caché** en `st.session_state` por `hash_lote` (el de T1, SHA-256 del lote canónico) + hash de parámetros; se recalcula solo si cambia el archivo o la configuración de anomalías; se limpia con "Nuevo análisis". Todos los valores dinámicos pasan por `h()`, `html_block` o `render_html_table`.
- Manual de usuario: viñeta en 5.1 Imperator Diagnostics.

### Evidencia (contenedor cloud, Python 3.11, numpy 2.4, pandas 3.0)
- `python3 -m unittest discover -s tests`: **236 pruebas en verde** (218 previas + 18 nuevas en `tests/test_anomalias.py`): reproducibilidad con semilla (misma semilla = mismo resultado; semilla distinta = distinto), puntaje en [0, 1] y atípicos arriba, columnas constantes, hash de parámetros estable, validación de percentiles en `ParametrosAnomalia` y en `AmlConfig` (claves nuevas aceptadas, valores fuera de rango rechazados), **detección de 15 clientes inyectados** (fraccionamiento nocturno en efectivo bajo el umbral RTE) con precisión >= 0.8 en el top 15 y nivel Alto en >= 80 % de ellos, reproducibilidad del resultado completo, explicabilidad coherente (3 variables ordenadas por |z|, dirección correcta, valor igual al de la matriz de variables, texto con la etiqueta), percentiles y niveles, **el Score IMPERATOR, el Nivel de Riesgo y el DataFrame procesado no cambian** tras calcular y marcar, puntos ciegos, pocos datos (6 clientes: método MAD y el inyectado en primer lugar; 1 transacción), columnas faltantes (sin Fecha, Cliente_Destino, UbicacionRiesgo, TipoOperacion, Perfil) con nulos y textos en Monto, señal desactivada por configuración, metadata de trazabilidad, variables por transacción (cerca de umbral, redondos, fin de semana, hora atípica) y lote real de ejemplo.
- **Rendimiento**: `Transacciones_AML_200.xlsx` (200 transacciones, 130 clientes): 0.41 s. Conjunto sintético de **50,300 transacciones y 5,015 clientes**: **1.99 s** (clientes + transacciones), método `ensamble_if_mad`, precisión 0.96 en el top 25 sobre 25 anomalías inyectadas, resultado idéntico en dos corridas.
- Prueba de humo `apptest_smoke.py` en Imperator Diagnostics, Análisis por Cliente, Casos de Alerta, Matrices de Riesgo, Configuración, Resumen Ejecutivo, Informes y Reportes y SIN_DATOS: 0 excepciones, 0 errores, sin XSS. Prueba adicional con `AppTest`: la pestaña renderiza "puntos ciegos" y "Ranking de anomalía", `casos` en sesión contiene `Anomalia_Percentil` y `Anomalia_Nivel`, la clave de caché existe; generación real del Informe Ejecutivo PDF (224 KB) y verificación con pypdf de que contiene el párrafo de la señal y la línea de trazabilidad.
- Duplicación de código: 1.46 % (umbral 10 %).

### Limitaciones documentadas (también visibles en la UI)
- No supervisado: "anómalo" significa distinto de la mayoría del lote, no sospechoso; exige examen humano (Art. 29 Ley 6593).
- Percentiles relativos al lote: con pocos clientes (menos de 12 se usa MAD) o lotes homogéneos pierde poder discriminante; un cliente puede ser anómalo por estar por debajo de la cartera (la dirección se muestra para descartarlo).
- Deriva entre períodos y sin memoria histórica: comparar con el expediente.

### Buenas prácticas de gobierno del modelo
- Validación inicial contra casos examinados y alertas de reglas; registrar la precisión observada en el top 25 antes de usarla para priorizar.
- Recalibración trimestral (o al cambiar la cartera o umbrales RTE/FEIC) de percentiles, umbral de punto ciego y variables; cualquier cambio de variables o fórmula debe incrementar `VERSION_MODELO`.
- Trazabilidad: versión, método, parámetros (hash) y hash de lote en la pestaña, la ficha del cliente y el Informe Ejecutivo; incluir la línea en el fundamento del caso.
- Independencia: la señal no altera el Score IMPERATOR ni los estados; su uso para escalar un caso pasa por el flujo de cuatro ojos de T1.

### Variables nuevas
Ninguna de entorno. Claves de configuración AML: `anomalia_activa`, `anomalia_percentil_alto`, `anomalia_percentil_medio`, `anomalia_n_arboles`, `anomalia_semilla`, `anomalia_score_punto_ciego` (con valores por defecto; los archivos `.saml` y JSON antiguos siguen siendo válidos).

### Pasos de despliegue
1. Ninguna migración ni dependencia nueva (solo numpy y pandas ya presentes).
2. Opcional: si en el futuro se desea scikit-learn, añadirlo a `requirements.txt` documentando el impacto en la imagen (más de 100 MB) y mantener la implementación numpy como fallback; el módulo no lo requiere.

### Pendientes y motivo
- **Señal por transacción en la vista Transacciones**: se calcula (`Anomalia_Tx_Percentil`) y se muestra en el detalle del cliente, pero no se añadió como columna en la bitácora transaccional para no ampliar una tabla ya densa; opción: columna opcional con filtro.
- **Memoria histórica entre lotes** (percentiles frente a períodos anteriores) requeriría persistir variables por cliente y lote en base de datos; queda como evolución con tabla `AnomaliasHistorial` (licenciaid, hash_lote, cliente, variables, percentil) y comparación de deriva.
- **Contribuciones nativas del bosque** (profundidad por variable) se descartaron en favor de la desviación robusta por ser más estable y legible; puede añadirse como segunda vista técnica si el Oficial la solicita.

---

## T9 Pendientes UI

**Estado:** HECHO · **Fecha:** 17/09/2026 · **Commit:** `1aedba4` (código) y el commit de este informe.

### Qué se hizo
- **Moneda configurable en todos los PDF** (`frontend/mod_reportes.py`): los cuatro documentos (ficha de cliente, informe ejecutivo, RTS y RTE) usan `fmt_moneda`, `etiqueta_monto` y la nueva `nombre_moneda()` de `ui_components`; la portada de cada PDF indica "Moneda de trabajo: GTQ (Quetzales)" o "USD (Dólares)". Ejes `set_ylabel` de matplotlib ("Monto total (Q)" fijo) y la columna "Volumen (Q)" pasan por `etiqueta_monto`; el KPI "Volumen total" del informe ejecutivo se formatea con `fmt_moneda` (antes número sin símbolo).
- **Monto normativo frente a moneda de trabajo**: `ui_components.UMBRAL_RTE_USD = 10_000` y `UMBRAL_RTE_TEXTO = "USD 10,000"` son la única definición del umbral del Art. 31 (`mod_transacciones` la reutiliza). El RTE muestra el KPI "Umbral normativo (USD)" junto a "Monto total efectivo (Q|US$)" y un párrafo (`texto_moneda_normativa()`) que explica que el umbral es un monto normativo en dólares comparado contra su equivalente en la moneda de trabajo. El aviso de Transacciones, la pestaña RTS/RTE y el manual (sección 4) repiten la distinción. Con moneda USD el texto del PDF no contiene ninguna "Q"; con moneda GTQ, "USD" aparece solo en el umbral normativo.
- **Ejes, hovertemplates y textos de la UI**: `mod_resumen` (3 títulos de eje y 4 hovertemplates), `mod_cliente` (4 hovertemplates), `mod_configuracion` (10 etiquetas y textos de umbrales, incluido `monto_critico` del resumen), `mod_imperator_diagnostics` (2), `mod_red_transaccional` (1), `mod_alertas` y `mod_matrices` ("Total Mensual"), `mod_anomalias` y `backend/anomalias.py` (símbolo `US$` coherente con `simbolo_moneda`, antes `USD `). El símbolo se toma una vez por vista (`sim = ui_components.simbolo_moneda()`).
- **Estilos inline** (`frontend/theme/styles.css`, `ui_components.py` y 16 módulos): los atributos `style=` en bloques HTML del código pasan de **156 a 2** (los dos restantes son valores numéricos dinámicos: altura máxima de `render_html_table` y tamaño de fuente opcional de `kpi_card`). Mecanismo: clases de tono `.tone-danger|warn|yellow|ok|green|info|violet|accent|muted|strong|white|sky` que fijan la variable CSS `--sv-tone` (y `--sv-tone-soft`) a partir de los tokens; los componentes (`.level-card`, `.rule-row-state`, `.client-head`, `.catalog-card`, `.frame-card`, `.badge-tone`, `.badge-soft`, `.panel-accent`, `.strategy-panel`, `.group-card`, `.kicker`, `.td-code`, etc.) leen esa variable para color, borde o fondo. `ui_components.tone_class(color)` traduce un color de token a su clase y **nunca emite el color crudo** (desconocido -> `tone-muted`), por lo que los colores dinámicos (nivel de riesgo, categoría de acción, estado) ya no requieren `style=`. Utilidades con tokens: `.tx`, `.tx-strong`, `.tx-muted`, `.fw-600/700`, `.mono`, `.fs-12/13`, `.mt-*`, `.mb-*`, `.w-*`. `spec_card` y `nivel_badge` también quedaron sin `style=`. Se añadió la regla `.section-badge` que `regla_titulo` usaba sin definir.
- **Corrección encontrada durante la migración** (`frontend/mod_mitigacion.py`): la tabla "Acciones asignadas por cliente" se insertaba con `h(filas_acc)`, de modo que el HTML de las filas (ya escapadas celda a celda con `_texto_seguro`) se mostraba como texto (`&lt;tr&gt;...`). Ahora las filas se construyen como `filas_acc_html` y la tabla se renderiza; la prueba estática de `test_ui_safe` sigue en verde porque cada celda pasa por `_texto_seguro`.
- Manual de usuario: viñeta "Moneda de trabajo y monto normativo" en la sección 4.

### Evidencia (contenedor cloud, Python 3.11)
- **Medición antes/después** (regex `[ ]style=["']` sobre `app.py` y `frontend/*.py`): 156 -> 2 (-98.7 %; objetivo: al menos la mitad). Por archivo: `mod_configuracion` 45 -> 0, `mod_mitigacion` 26 -> 0, `mod_resumen` 17 -> 0, `mod_red_transaccional` 10 -> 0, `app.py` 10 -> 0, `mod_reportes` 9 -> 0, `mod_matrices` 9 -> 0, `mod_cliente` 6 -> 0, `mod_imperator_diagnostics` 5 -> 0, `ui_components` 4 -> 1, `mod_anomalias` 4 -> 0, `mod_mfa` 3 -> 0, `mod_ubicaciones` 3 -> 0, `mod_riesgo_ldft` 2 -> 0, `mod_utils` 1 -> 1, `mod_alertas` y `mod_transacciones` 1 -> 0. En el HTML renderizado con `AppTest` (17 vistas): Configuración 84 -> 0, Matrices 50 -> 1, Acciones de Mitigación 2143 -> 0, Imperator Diagnostics 26 -> 16 (los KPI con tamaño de fuente), Resumen 17 -> 2, Red 11 -> 2, Reportes 9 -> 0.
- **Sin cambios visuales**: se volcó el HTML de las 17 vistas de la navegación antes y después (`AppTest`, admin, `Transacciones_AML_200.xlsx`, moneda GTQ) y, tras eliminar los atributos `style=` y `class=` y el bloque `<style>`, el contenido es **idéntico** en 14 vistas; las tres diferencias son intencionales: Acciones de Mitigación (la tabla de acciones ahora se renderiza en vez de mostrarse escapada), Informes y Reportes (texto del aviso RTE con la aclaración de moneda) y Manual (viñeta nueva), más marcas de tiempo. Toda clase usada en HTML del código existe en `styles.css`/`login.css` (verificado por prueba; 160 clases, 0 sin definir). Las clases de tono definen colores exclusivamente con tokens ya validados por `test_contraste`; los únicos colores heredados fuera de token (`#2ecc71`/`#e74c3c` del estado del catálogo) se alinearon a `exito_texto`/`peligro`.
- **PDF en ambas monedas** (generación real por la UI con `AppTest`, botones "Generar PDF", "Generar Informe Ejecutivo PDF", `btn_rts`, `btn_rte`; texto extraído con `pypdf`): con GTQ, informe ejecutivo (218 KB) "Moneda de trabajo: GTQ (Quetzales)", "Q1,965,444 · Volumen total (Q)", 0 apariciones de "US$"; ficha de cliente (99 KB) "Moneda: GTQ (Quetzales)"; RTS (8 KB) "Moneda de trabajo: GTQ"; RTE (9 KB) "Umbral normativo: USD 10,000 o equivalente", "Q510,491 · Monto total efectivo (Q)", "USD 10,000 · Umbral normativo (USD)" y el párrafo de aclaración. Con USD: "US$1,965,444 · Volumen total (US$)", "Moneda: USD (Dólares)", "US$510,491 · Monto total efectivo (US$)", **0 apariciones de "Q"** en los cuatro documentos y el umbral sigue en "USD 10,000". Sin excepciones ni `st.error`. En la UI con moneda USD (9 vistas): 0 etiquetas "(Q)" o "Q<número>"; los umbrales de Configuración muestran "US$45,000", "US$20,000", "US$30,000".
- `python3 -m unittest discover -s tests`: **245 pruebas en verde** (236 previas + 9 nuevas en `tests/test_estilos_inline.py`): tope de `style=` inline (<= 10), clases HTML definidas en el tema, `tone_class` solo emite clases del tema (incluido color desconocido y `None`), `nivel_badge` sin `style=`, tonos con `--sv-tone`, formato y etiquetas por moneda (GTQ, USD y valor inválido), umbral RTE normativo presente en ambas monedas, símbolo coherente en anomalías y ausencia de "(Q)"/"Q{" fijos fuera de `ui_components`. `test_ui_safe` (escape) y `test_contraste` (AA) en verde. Humo `apptest_smoke.py` en las 17 vistas de la navegación más SIN_DATOS y LOGIN: 0 excepciones, sin XSS (el `st.error` de "Seguridad de la Cuenta" con usuario ficticio sin fila en `Licencias` es previo a T9 y se reproduce en el snapshot anterior).
- Duplicación: 13.596 líneas, 210 duplicadas, **1.54 %** (umbral 10 %).

### Variables nuevas
Ninguna.

### Pasos de despliegue
1. Ninguna migración ni dependencia. La moneda se elige en Configuración > "Moneda de presentación" y se guarda en la configuración AML (`.saml`/JSON antiguos siguen siendo válidos con GTQ por defecto).
2. Tras desplegar, generar un RTE y comprobar que la portada indica la moneda de trabajo y que el umbral aparece como "USD 10,000".

### Pendientes y motivo
- Los dos `style=` restantes son valores numéricos calculados (altura de tabla, tamaño de KPI); moverlos a clases exigiría un conjunto cerrado de tamaños. Opción: `.kpi-xl` y alturas fijas de tabla si se quiere llegar a cero.
- La conversión entre monedas no existe (no hay tipo de cambio): la moneda de trabajo etiqueta los montos tal como vienen en el Excel. Si un lote mezclara monedas, haría falta una columna de moneda por transacción y un tipo de cambio de referencia (Banguat) para comparar contra el umbral RTE; queda como evolución.
- Los gráficos matplotlib de los PDF se rasterizan: la etiqueta de eje no se puede verificar con `pypdf` (se verificó por código y por la generación sin errores).
